# 前端设计 — 客户端与宿主接入 v0_1

**版本**: v0.1
**状态**: 定稿候选（前端工程唯一设计依据；待后端主体修订对齐后转正）
**日期**: 2026-08-23
**上游**: 由《规划-DeepSeekHarness集成-认知插件-v0_2》（已存档）升级泛化而成
**关联**: 《Lethefield-设计文档》v1.7 §8/§9；《Lethefield-开发文档》v1.2 M5；《课题-dsh会话日志与EX事件流映射-v0_1》

---

## ⚠️ 给后端主体维护方的交接说明（先读这节）

本文档生效的前提，是后端主体（设计文档 / 开发文档 / M5 接口层）完成以下对齐修订。**修订动作由后端 Agent 执行，本文档不代为修改**：

| # | 待修订位置 | 修订内容 |
|---|---|---|
| B1 | 设计文档 §9（MCP / SDK 接口设计） | 主体对外界面**收窄为 HTTP + JSON API**（§8 技术栈表"API 层"行同步）；**MCP 封装降级为远期可选形态**，不占用 1.0 开发资源；§9 标题与内容中"MCP / SDK"表述调整为"对外 API（HTTP+JSON）"，原 MCP 相关段落标注"远期可选，见前端设计文档 §2.1" |
| B2 | 设计文档 §9 | 新增指向：**客户端（SDK / 宿主插件 / 宿主适配器）属前端工程，设计依据见《前端设计-客户端与宿主接入》**；主体不承载任何客户端代码 |
| B3 | 开发文档 M5（MCP / SDK 接口层） | 模块名与目标修订为"对外 API 层"；删除/冻结 MCP 封装的实施计划（SDK 侧不设独立模块，见本文档 §2.1）；M5 既有 HTTP+JSON 实现**不变**，本次仅收窄封装面 |
| B4 | 开发文档 M5 内遗留批注（"不能依赖 LLM 主动上传"一段） | 该批注结论已被本文档 §2.1 决策 D1 正式化，批注可勾除并留痕指向本文档 |
| B5 | README「仓库结构」`services/` 行 | `api（M5 接口层）`描述同步更新（提及 MCP 处改为对外 API） |

**修订原则**：不动 §9 已定的四类操作接口语义；不动 M5 既有代码；本次变更的性质是"收窄对外封装面 + 客户端划出主体边界"，属定稿修订，按决策留痕惯例记录版本变更。

---

## 1. 定位与边界

Lethefield 主体是认知服务后端，对外界面**只有 HTTP + JSON API**（设计文档 §9 四类操作接口，开发文档 M5 落地）。接口语义上分为两组：

```
接口组 1：事件摄入  ingest(events)      ← 宿主/客户端事件流 → EX
接口组 2：认知注入  retrieve(query_ctx) → 客户端上下文组装 ← RMS 塌缩结果
```

**一切客户端（SDK、宿主插件、宿主适配器）都是前端工程**：不属于主体仓库的服务边界，各自独立演进，仅依赖上述 API 契约。本设计文档是全部前端形态的唯一设计依据。

**认知必然性原则（前端设计的最高约束）**：retrieve 必须是宿主每次上下文组装时的结构性动作，不得依赖 LLM 临场判断是否调用。遗忘与塌缩不是"想起来才做"的事——凡把召回决定权交给模型的接入形态（如 MCP 工具），均不满足认知服务的定义。

## 2. 决策记录

### 2.1 已定决策

| # | 决策 | 内容 | 理由 |
|---|---|---|---|
| D1 | **MCP 封装降级为远期可选** | 1.0 不开发 MCP 形态；仅当未来出现仅支持 MCP、无法深度集成的宿主时再复活 | MCP 工具的调用时机由模型决定，违反认知必然性原则；且无法干预宿主上下文组装，接入后 Lethefield 退化为高级 RAG，FF 动力学旁落 |
| D2 | **SDK 保留，定位为 B 端用户入口** | 面向自建 Agent 的开发团队，Python 优先；SDK 是 API 的薄封装，不设独立后端模块 | B 端不经过任何现成 harness，需要三行代码接入 ingest/retrieve 的最低摩擦路径 |
| D3 | **宿主接入形态按宿主架构选型** | dsh → Cordis 插件（§3.2）；Codex → app-server 适配器（§3.3） | 插件"进入宿主体内"（控制力强、稳定性低）；适配器"在宿主耳边"（控制力弱、进程边界免费故障隔离）；见 §4 模式对比 |
| D4 | **实施顺序：dsh 先行** | Codex 适配器待 dsh 通路验证（§5 T7 对比实验出初步结果）后启动 | 只有 dsh 的上下文注入 seam 支持内部替换，才能做 FF 开/关的干净对照实验 |

### 2.2 模式对比与故障语义（设计依据，存档备查）

| | Cordis 插件（dsh） | app-server 适配器（Codex） | SDK | MCP（已降级） |
|---|---|---|---|---|
| 进程位置 | 宿主进程内（控制反转） | 独立进程，JSON-RPC | 用户进程内 | 独立进程 |
| 事件摄入 | 可全量监听 | 可全量监听 | 用户代码显式调用 | 依赖模型主动调用 ✗ |
| 上下文注入 | 内部替换 seam（必然） | 协议层旁路（需站在链路上：作为客户端或中间代理） | 用户代码显式调用 | 无注入点 ✗ |
| Lethefield 宕机后果 | **由插件容错设计决定**：熔断+回落默认注入则宿主无感；同步阻塞则卡死宿主事件循环（最恶劣死法，红线禁止） | 宿主不受影响（进程边界免费隔离） | 用户程序自行处理 | 工具调用失败 |
| 决定权 | 宿主运行时结构 | 链路位置 | 开发者代码 | **模型临场判断** ✗ |

## 3. 客户端设计

### 3.1 共性红线（所有客户端形态适用）

1. **space 隔离**：宿主的租户单位（workspace/session/用户应用）与 space_id 严格一对一映射，禁止跨 space 共享；违反即触及设计文档 §0.5 公理 3 结构性越界。
2. **可关闭、可回退**：每个客户端必须可整体禁用，宿主/用户程序回落到无认知层行为；此开关同时是对比实验（FF 开/关）与故障回退的依赖。
3. **摄入异步、读取同步**：事件摄入一律异步（失败可重试、不阻塞宿主）；retrieve 在同步路径上，必须带超时熔断。
4. **依赖白名单**：只依赖宿主公开接口/协议，禁止 import 宿主内部实现；宿主破坏性变更时损失限定在壳层。
5. **事件 schema 差异收敛在客户端内**：各宿主事件模型 → EX 事件模型的映射表由客户端维护，不进主体。

### 3.2 客户端 A：dsh Cordis 插件（先行）

宿主：DeepSeek Harness（dsh），一切皆插件，Capability Seam（Definition/Provider/Consumer）。

```
dsh 运行时（Node/TS）
 └─ lethefield-plugin（Cordis 插件，薄层）
     ├─ 监听会话事件流 → POST 至 ingest（异步）
     ├─ 实现上下文注入 seam → 调 retrieve，注入塌缩结果（同步，带熔断）
     └─ space_id ← dsh workspace/session 一对一映射
          │ HTTP（本地或内网）
Lethefield 全栈（docker compose，主体零改动）
```

- **延迟预算**：retrieve 位于每次模型请求的同步注入路径，主体全栈 P99 将叠加到每轮对话；插件层预留本地缓存位，延迟实测后定预算数值（T6）。
- **故障语义**：插件内 catch 一切异常 + 超时熔断 + 回落 dsh 默认上下文组装；禁止任何同步阻塞调用。
- 概念映射与待决问题（事件粒度、显著性六维支撑度、fork/replay 语义等）见《课题-dsh会话日志与EX事件流映射-v0_1》。

### 3.3 客户端 B：Codex app-server 适配器（后置）

宿主：Codex harness（Rust 核心 + app-server，JSON-RPC 协议：持久会话、实时事件流、人工审批回调）。

- **形态**：JSON-RPC 客户端/中间层。监听事件流 → ingest；注入只能协议层旁路——适配器作为客户端时可自由改写请求；若要干预官方客户端（IDE 插件等）发出的请求，须站在链路上做中间代理（多一跳、多一层脆弱性，需评估官方客户端是否允许改 server 地址）。
- **故障语义**：Lethefield 宕机 → 注入缺席，宿主会话不受影响。
- **控制力评估**：进不了宿主内部上下文组装逻辑，retrieve 注入为"additive"（附加）而非"替换"；这是与 dsh 插件的本质差距，也是 D4 排序的依据。

### 3.4 客户端 C：Python SDK（B 端入口，与 A 并行）

- API 的薄封装：ingest / retrieve 两组方法 + 鉴权 + 重试；零额外语义。
- 目标体验：三行接入（init → ingest → retrieve）。
- 非 Python 的 B 端需求出现前，不多语言扩张（沿用主体"1.0 不做多语言分层"原则）。

## 4. 演示与评测（前端侧）

首个可公开演示场景（MVD）：同一 coding 任务流在 30 天尺度上，dsh + 插件（FF 开）对比 dsh 默认日志检索（FF 关），指标 = 召回质量 / 噪声率 / token 成本。同时设计加速时间标尺变体（压缩事件密度的短周期版本），避免短期 demo 中"遗忘"被误读为 bug。

## 5. 待办清单（未排期）

- [ ] T1：调研 dsh 插件开发文档与 Cordis Service/Context/Event 模型，确认会话事件流与上下文注入 seam 的公开接口签名
- [ ] T2：dsh 插件骨架设计（目录结构、事件监听清单、retrieve 注入点接口签名）
- [ ] T3：space_id ↔ dsh workspace/session 映射方案（候选：ControlPlaneStore 映射表扩展，需后端配合评审）
- [ ] T4：dsh 事件 schema → EX 事件 schema 映射表（见关联课题文档）
- [ ] T5：插件最小实现：摄入通路先行（只写不读），验证事件流完整性
- [ ] T6：retrieve 注入通路接通，同步路径延迟实测，定延迟预算数值
- [ ] T7：MVD 对比实验设计与执行（§4）
- [ ] T8：故障回退演练：主体宕机时 dsh 回落默认注入、不阻塞会话
- [ ] T9：API 接口契约文档（ingest / retrieve 签名 + 事件 schema 最小公约数，三类客户端共用）——**需后端主体确认后冻结**
- [ ] T10：Codex app-server JSON-RPC 协议调研：事件流字段、审批回调与 EX 事件映射可行性
- [ ] T11：Codex 适配器立项评审（依赖 T7 初步结果）
- [ ] T12：Python SDK 最小实现（依赖 T9 契约冻结）

## 6. 风险

- **R1（高）**：dsh Developer Preview 破坏性变更 → 红线 4 白名单 + 壳层极薄。
- **R2（高）**：同步 retrieve 延迟不可接受 → T6 先行实测；退路为预取缓存 + 降级异步提示。
- **R3（中）**：dsh 事件流字段不足以支撑 SS 六维显著性评分 → 课题文档跟踪；退路为首版 FF 仅启用事件视界距离 + 墙钟衰减。
- **R4（中）**：MVD 周期长，短期 demo 中遗忘表现为疑似 bug → §4 加速时间标尺变体。
- **R5（中）**：Codex 适配器只能 additive 注入，FF 效果可能被宿主原生上下文组装稀释 → T10 调研时实测注入内容的实际呈现权重。

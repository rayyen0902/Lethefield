# ConfigurationManagementGraph 自身的存储配置
# 后端指向 cell 的 Cassandra + 图索引/向量用 ES（compose 服务名）。
# ids.authority.wait-time 保持 JanusGraph 默认值，禁止调大（红线 4）。
gremlin.graph=org.janusgraph.core.JanusGraphFactory
storage.backend=cql
storage.hostname=cassandra-cell
storage.cql.keyspace=janusgraph_config
index.search.backend=elasticsearch
index.search.hostname=es-graph
index.search.elasticsearch.health-refresh-interval=5s
cache.db-cache=false

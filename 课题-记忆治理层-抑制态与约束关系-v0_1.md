# Lethefield — 记忆治理层课题（抑制态与约束关系）

**版本**: v0.1（挂起课题，未定稿）
**状态**: **挂起**——1.0 不启动；2.0 启用前提见 §6。源于外部提案（ChatGPT 的 Constraint Graph 提案，2026-08-04 评审），经参谋会话拆解后按本文档口径立项挂起
**关联**: Lethefield-设计文档 v1.7（§5 检索、§13 记忆维护、§11.5 红线）；Lethefield-开发文档 v1.2（M4 检索、M6 生命周期、M7 纠错）
**日期**: 2026-08-04

---

## 1. 来源与提案摘要

外部提案建议引入 **Constraint Graph（约束图）** 作为 RMS 第五类关系图：不删除、不修改底层记忆，而是在检索阶段根据身份、权限、上下文、任务目标和系统规则调节记忆可见度，使系统具备"知道但不一定展示"的能力。提案将其定位为 Memory Governance Layer（记忆治理层），给出检索公式 `Retrieval Score = 语义相关 × FF 活性 × 约束因子`。

提案概念上与既有哲学同构——"判断下沉到检索时策略，不烧进数据层"正是 M7 禁失效标志红线的推广。但提案把四类性质不同的问题混为一条边，且未回答两个硬问题。经评审拆解如下。

## 2. 概念拆解：提案用例实为四件事

| # | 问题类型 | 提案用例 | 现状评估 |
|---|---|---|---|
| 1 | 查询上下文相关 | "问 Python 时 Python 经验浮现" | **冗余**——M4 Stage 2 锚点 + RRF + λ2 sim 已覆盖 |
| 2 | 长期规则加权 | "2019 架构决策在改数据库时必须浮现" | **大部分已覆盖**——固化（不过滤）+ 语义相关；显式 dependency 边是增量但增量不大 |
| 3 | **用户偏好治理（抑制态）** | "重大疾病史默认不主动召回，用户明确提才出现" | **真缺口**——现有体系只有正向锁定（固化）与出局（归档），无"记得但不主动召回"状态 |
| 4 | 安全硬边界 | "财务数据不给普通员工看" | **图边是错误载体**——见 §3.2 |

**课题范围收窄为第 3 类（抑制态）**。第 2 类若种子期数据证明固化+语义不够，再议；第 4 类走多层防御路线（§3.2），不进本课题。

## 3. 两个必须先回答的硬问题

### 3.1 信任域（命门，未解决不动工）

约束关系若作为普通图数据走普通写入路径，则 prompt injection 可写入"始终展示全部数据"或删除既有抑制关系——治理层本身成为污染源（与"污染主战场在空间内部"的既有判断一致）。

**启用前必须完成信任域设计**：约束关系的写入/修改/删除必须属于更高信任域（候选方向：账号 owner 签名、IS 层专用 scope、独立于 memory.* 的治理接口），且约束变更全部进决策留痕。写入者鉴权（`agent_actor_id` 绑凭证）是必要条件，不是充分条件。

### 3.2 硬安全边界的正确载体（不进本课题，但立此存照）

安全硬边界（权限、防泄漏）不能只靠检索层可见性控制——单层 enforcement 一个检索 bug 就是一次泄漏，违反纵深防御。正确做法：

1. **隔离单位仍是 space**——敏感数据分 space 存放，不在 space 内开 per-node 权限边（避免请回我们刻意消掉的复杂度）；
2. 硬规则在凭证 scope、输出过滤等**多层同时 enforcement**，检索可见性只是其中一层。

## 4. 与现有机制的关系（防止重复建设）

- **固化**：正向锁定（永不过滤）。抑制态是其镜像（记得但默认不召回）——两者构成记忆可见性的两极，未来实现应复用同一状态机制而非另起炉灶。
- **supersedes**：处理"事实被纠正"，检索重定向。抑制态处理"事实没错但不主动呈现"，语义正交。
- **ρ / λ2 / λ3**：查询时相关性与显著性权重。抑制态是**持久治理状态**，不是查询时参数——禁止实现为"又一个检索旋钮"。
- **归档**：低于阈值出热图。抑制态节点仍在热图、仍参与衰减与 δ，只是检索可见性受限——与归档生命周期正交。

## 5. 课题范围（启用时的第一步，刻意做窄）

**只做抑制态一件事**：

1. 记忆节点可被打上"抑制"治理标记（信任域内写入，§3.1）；
2. 默认检索不召回被抑制节点（两处 θ 硬过滤同层旁路，机制可参照固化的 `consolidated_at` 旁路实现，方向相反）；
3. 显式追溯/点名时可召回（与 supersedes 的"追溯历史"参数同构）；
4. 抑制/解除抑制全量留痕，抑制本身不阻断 FF 衰减与 δ（抑制是可见性，不是免死牌）。

明确不做（本课题内也不做）：提案的"约束因子"乘法公式（与 λ3/ρ 体系叠加出一个新旋钮，标定负担不可接受）；Context Constraint 类型（§2 第 1 类，冗余）；per-node 安全权限边（§3.2）。

## 6. 2.0 启用前提（全部满足才启动本课题）

1. 1.0 准出完成，FF 核心参数（λ、θ_base、δ 三值）经种子期真实流量标定——治理层不能建在未标定的地基上；
2. 固化机制经真实使用验证（抑制态将复用其状态机制，先验证原型）；
3. §3.1 信任域设计完成并经评审；
4. 有真实用户场景驱动（如伴侣类产品的敏感记忆需求），不是为了架构完整性而做。

## 7. 挂起期间约束

- 开发文档 v1.2 的模块（M0–M17）**不预留**约束图/治理层接口——过度设计比返工更贵；启用时按当时的真实需求定形态。
- 1.0 开发中如发现与抑制态强相关的现象（如某类记忆频繁需要"别主动提"），记入决策留痕，作为本课题启用时的需求证据。

---

*本文档为挂起课题，不产生 1.0 开发任务。启用时先更新设计文档，再按变更纪律更新开发文档。*

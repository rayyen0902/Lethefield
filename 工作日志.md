# 工作日志

每个模块在独立会话中开发，本日志供新会话快速续接。长期有效的约定与环境事实以 `AGENTS.md` 为准，此处只记过程、决策与遗留。

## M0 — 工程地基（2026-08-04，commit f7a7bda，CI 绿）

**交付**
- 仓库骨架：uv workspace monorepo（Python 3.12 托管）+ ruff/pytest 基线 + Makefile；git init，仓库公开于 GitHub（rayyen0902/Leth0field）。
- 共享库三样（M0 纪律：共享代码只许这三样，各有独立单测）：
  - `libs/logschema`：结构化日志事件 schema（Pydantic，JSONL 序列化；`space_id` 在明细事件中合法，仅禁作聚合指标标签）。
  - `libs/metrics`：指标 registry 封装，命名 `lethefield_<域>_<名称>_<单位>` 与标签白/黑名单在注册时强制（`space_id`/`node_key` 直接抛错）。
  - `libs/clients`：五个客户端工厂（env 注入）+ **`ControlPlaneStore` 冻结接口**（契约 4）+ 单节点 `StaticControlPlaneStore`。
- `ops/decision_log`（§11.3）、`ops/auth_registry`（§12.4，粒度 scope 判定）最小实现：PostgreSQL 存储 + CLI。
- `docker-compose.yml` 单节点全栈 8 组件：JanusGraph 1.0.0 + Cassandra×2（cell/ex）+ ES×2（graph/ops）+ Pulsar + Redis + PostgreSQL；JG 启用 ConfigurationManagementGraph 支持动态建图。
- `tests/integration`：spike q1–q4 按文档重建（spike/ 不在仓库）——图读写 2 跳、向量索引、动态建图冷开、**四断言**（高分召回/低分过滤/衰减过滤/跨 space 零泄漏），衰减断言复刻 spike 场景（s=0.9、Δn=20 → s_eff≈0.098 被 θ 过滤）。
- `scripts/ci.sh` + GitHub Actions 工作流。
- 验证：本地 24 单测 + 10 集成全绿；GitHub Actions 一次通过（干净 ubuntu 从零起栈）。

**关键决策**
- Python 依赖管理用 uv workspace；lint 只要 ruff（lint+format），不引入 mypy（M0 保持轻）。
- `ControlPlaneStore` 接口 M0 冻结、实现随 M9——但附 `StaticControlPlaneStore`（全部映射到单一本地 cell），否则 M1+ 各模块在单节点形态下无法落地"存储访问必经抽象"这条硬约束。
- 决策留痕/授权注册表存储用 compose 里的 PostgreSQL（不用 SQLite，避免后续迁移）。
- 测试用 FF 参数占位：λ=0.16、θ_base=0.3、ρ=1（选值依据是复刻 spike 的 s_eff≈0.10 场景，正式标定属种子期）。

**踩坑（后续模块要沿用）**
- kNN 跨 space 零泄漏 = routing（分片收拢）+ `space_id` term filter（语义隔离）**双重机制**——只带 routing 会命中同分片的其他 space 文档（分片是多 space 共享的）。M4 Stage 2 必须沿用。
- Gremlin 绑定名避开保留字（`keys` 报 499）；`getGraphNames()` 等服务端返回值按元素逐个流回，不是嵌套列表。
- JanusGraph 1.0 顶点不支持 `v.both(Direction, label)` 直调，邻居扩展用 traversal `both()` 步骤。
- JG 官方镜像读取的 server yaml 固定路径是 `/etc/opt/janusgraph/janusgraph-server.yaml`（不是 conf/ 下的）；自定义 yaml 只绑 ConfigurationManagementGraph 后，gremlin 客户端别名必须指向它。
- Cassandra 官方镜像重启后 `broadcast_rpc_address` 失效（启动报 wildcard rpc_address 错），compose 必须显式设 `CASSANDRA_BROADCAST_RPC_ADDRESS`。
- spike 遗留容器（spike-elasticsearch 等）占用 8182/9042/9200 端口 + colima 曾因 8 容器并发启动卡死 daemon——重启 colima 解决；spike 容器已停未删。

**遗留**
- spike 原始脚本（q1–q4、SPIKE_REPORT.md）不在仓库，四断言是按开发文档/设计文档 §16 描述重建的；拿到原脚本后可对比参数口径。

## M1 — 存储基础设施（2026-08-04，commit b47ac08，CI 绿）

**交付**
- compose 集群命名：Cassandra `lethefield-cell`/`lethefield-ex`、ES `lethefield-graph`/`lethefield-ops`（改名需清卷重建——Cassandra 校验已存集群名）。
- `scripts/verify_isolation.py`：4 类存储物理隔离证明（Cassandra cluster_name+host_id、ES cluster_name+cluster_uuid、Pulsar admin 集群列表）。
- `scripts/check_graph_config.py`：`ids.authority.wait-time` 默认值巡检（红线 4）——静态（配置文件无显式配置）+ 运行时（逐动态图读 management 生效值）双层。
- `ops/clock_monitor`：时钟偏移监控告警（红线 6）——8 组件时钟采集（PG now()、Redis TIME、Cassandra toUnixTimestamp、Pulsar Date 头、ES 走 `docker compose exec date`、JG gremlin `new Date()`），±5s 阈值，告警输出结构化日志事件、退出码 1。
- `scripts/measure_restart_baseline.sh` + `deploy/baselines/restart_baseline.jsonl` 实测：JG 58s（spike 参考 ~52s）、ES ~40s、Cassandra ~28s、Pulsar 17s、Redis/PG ≤1s。
- `tests/integration/test_m1_infra.py` 6 项：隔离证明、红线 4 双层、健康偏移、+60min 模拟跳变告警、采集失败不静默、注入伪造源端到端。
- 验证：本地 24 单测 + 16 集成全绿；GitHub Actions CI 绿（run 30938772657）；巡检三脚本各跑一遍留证。

**关键决策**
- 时钟监控判定（`check_offsets` 纯函数）与采集（`collect_all`）分离——模拟跳变靠注入伪造样本/伪造采集器，不需要真的拨时钟；"采集失败"记为超大偏移样本触发告警，不静默跳过（与 Dead Man's Switch 同源）。
- `ids.authority.wait-time` 默认值以实测为准：JG 1.0.0 是 **300ms**（management toString = `PT0.3S`），不是凭印象的 100ms。巡检比较的是"等于默认值"，不是"等于某个文档数字"。
- ES 的 HTTP 响应没有 `Date` 头——其时钟采集改走容器系统时钟（`docker compose exec date`），语义上红线 6 监控的本来就是节点时钟。

**踩坑**
- JanusGraph Duration 的 toString 是 ISO-8601（`PT0.3S`），解析要兼容 `PT0.3S` 和 `100 ms` 两种形态（check_graph_config.py `duration_to_ms`）。
- dataclass 没有 `_replace`（那是 namedtuple 的），用 `dataclasses.replace`。

**遗留**
- 重启基线是手动脚本不进 CI（基线记录非质量门）。
- NTP 硬化本身是部署侧清单项（真实服务器部署时的配置要求），代码侧只能交付偏移监控告警；上服务器时需在部署文档中补齐 NTP 配置项。

## M2 — RMS 图 Schema（2026-08-04，commit 960600b，CI 绿）

**交付**
- 新 workspace 包 `services/rms`（`lethefield_rms`）：RMS 侧代码归属地。
  - `ensure_schema.groovy` + `schema.py`：幂等 schema 初始化（16 属性键 / 5 边标签 / byNodeKey 唯一 + byEntityKey 非唯一复合索引），CLI `python -m lethefield_rms.schema <gname>`。
  - `writer.py`：`create_event_node`（φ 初始化 n_last_touched=n_created、三计数器 0）、`create_entity_node`、`create_edge`——M15 写入链地基。
  - `vectors.py`：`rms_vectors` 独立向量索引建索引/写入/kNN（routing + space_id term filter 双机制强制）。
- `libs/clients` 新增 `gremlin_client()` 工厂（env `LETHEFIELD_GREMLIN_URL`）。
- `scripts/check_rms_schema.py`：schema 巡检 + rms_vectors mapping 校验 + ref_ex 抽样。
- `tests/integration/test_m2_schema.py` 6 项集成测试；conftest 重构为复用 lethefield_rms（spike 内嵌 schema 脚本已删，单一事实源）。
- 验证：本地 lint / 30 单测 / 22 集成测试全绿；GitHub Actions CI 绿（run 30945616671）。

**关键决策**
- `agent_actor_id` 做一等属性（设计文档 §8 明确其为节点属性），A_i 其余结构化内容进 `attrs` JSON 字符串。
- 实体顶点用 `node_type="entity"` + `entity_key` 区分，不引入自定义 vertex label。
- 向量索引 dims 参数化（ embedding 模型未定），测试用 dims=4。

**踩坑（后续模块要沿用）**
- gremlin_python 把 Python int **一律按 int32 序列化**，毫秒时间戳等大数直接溢出。Long 属性绑定改字符串传输 + Groovy `as long` 强转（writer.py 已是范例，M3/M15 照此写）。
- `lethefield_rms/__init__.py` 不做 eager 汇出（避免 `python -m` 双重导入告警），调用方一律按子模块导入。

**遗留**
- `ref_ex` 校验只覆盖 RMS 侧不变量（非空/字符串/图内唯一），EX 侧 join 待 M10 落地后补（check_rms_schema.py docstring 已注明）。
- 历次集成测试图（`m2_*`/`it_q3_*`/`it_m2_cli`）按红线 5 只 close 未 DROP，本地卷用 `make reset` 统一清。

## M3 — FF 计算引擎（2026-08-05，本地 lint + 53 单测 + 29 集成全绿；CI 已确认全绿）

**交付**
- `lethefield_rms/ff.py`（FF 引擎单点，公式/δ 逻辑禁止他处内嵌副本）：
  - 纯函数：`s_effective`（读取时现算的唯一入口）、`theta_effective`、`n_star`、`n_star_horizon`、`clamp_s`（截断集中在此，任何截断必计 `lethefield_ff_s_clamp_total{bound}`）。
  - `FFConfig`（frozen）：agent 常数层 λ=0.16 / N_neglect=20 / t/t₀=1.0 / θ_base=0.3 / s∈[0,1]——全是占位值，正式标定属种子期；ρ 是查询时参数，不进 config。
  - 三条独立 δ 触发路径（不合并且不做成通用打分接口）：`apply_reinforce`（+0.2，同步，touch）、`apply_conflict`（−0.5，consolidation 异步，touch）、`apply_neglect`（−0.1，sweep 异步，**不 touch**）；均立即重算 n_star_cached；`read_phi` 读 φ 快照。
  - δ 落库 Groovy 脚本只持久化引擎算好的结果（公式与截断全在 Python 侧，Groovy 不算 FF）。
- `ff_s_clamp_total{bound}` 指标注册进 prometheus 默认 registry（M12 统一接线暴露口）。
- `tests/integration/ff_utils.py` 改为引擎薄委托（q4 四断言口径不变、仍全绿）。
- 单测 `services/rms/tests/test_ff.py` 23 项（公式手算对照、Δn=0/λ=0 边界、触顶/触底截断+指标、参数分层）；集成 `tests/integration/test_m3_ff.py` 7 项（三 δ 路径图语义、s 只在 δ 时刻变化、spike 衰减过滤场景端到端、upper/lower clamp 指标）。

**关键决策**
- `n_star_cached` 存**绝对遗忘视界** n_last_touched + ceil(n*)：开发文档粗筛 `WHERE n_star_cached > $n_now` 只有与绝对事件序号可比才成立（存相对 n* 的读法自洽不了）；ceil 取整，粗筛宁可多留不可误杀。λ=0（不衰减）视界 +∞ 落 Java Long.MAX。
- s 合法区间取 [0,1]：设计文档未明文给定，按显著性语义设定并做成 FFConfig 可配字段（s_min/s_max），标定阶段可调整。
- θ_base=0.3 仅作 n_star_cached 重算的默认 θ；查询时 θ_effective=θ_base/ρ 由检索请求传入，两层在代码上物理分离。
- clamp 计数放在 `clamp_s` 内部而非各 δ 路径：截断只可能经此一处发生，不存在"忘了计数"的路径。

**踩坑（后续模块要沿用）**
- `libs/metrics` 的 `registry=None` 是 prometheus_client 原义——**不注册进任何 registry**，`REGISTRY.get_sample_value` 查不到；模块级指标必须显式传 `prometheus_client.REGISTRY`。
- prometheus_client 的 Counter 名以 `_total` 结尾会被内部剥离再在样本侧还原，`get_sample_value` 仍按全名查——无需特殊处理，知道即可。

**遗留**
- reinforce 的 EX 轻量元事件追加（fire-and-forget、时间窗合并，§13.7）由调用方负责，待 M5/M7 落地对接。
- "s 只在 δ 触发时刻变化"由集成测试覆盖；跨模块的静态红线扫描（确认无其他路径写 `s`）归 M13 红线自动化。
- M3 图 `m3_*` 按红线 5 只 close 未 DROP，`make reset` 统一清。

## M4 — 检索流程（四阶段）（2026-08-05，本地 lint + 73 单测 + 36 集成全绿；CI 已确认全绿）

**交付**
- `lethefield_rms/retrieve.py`（四阶段检索单点）：`retrieve()` 主入口 + `_stage2_anchors` / `_stage3_traverse` / `_stage4_budget`；结果结构 `RetrievalResult(nodes, edges)`——召回单元是带边子图（含 supersedes 边如实返回），FF 内部字段的对外裁剪留给 M5 debug scope。
- `vectors.py`：`rms_vectors` 加 `content` 文本字段（Stage 2 关键词一路的前提；旧索引经 `put_mapping` 幂等补齐）；新增 `keyword_search`（match + term filter + routing，与 kNN 同款双机制）。
- `writer.py`：`create_event_node` 的 `n_star_cached` 默认 None = 按 `ff.n_star_horizon` 自动计算（正确性兜底，M15 写入链默认正确）。
- 单测 `services/rms/tests/test_retrieve.py` 20 项（RRF 不掺 s、转移分软惩罚、supersedes 链解析/防环、束搜索注入式纯逻辑、Stage 4 双权重预算）；集成 `tests/integration/test_m4_retrieve.py` 7 项（对照五条验收标准 + q4 场景端到端 + 关键词一路）。

**关键决策（两处设计未覆盖，已升级确认）**
- **λ1·φ = 边类型先验权重** `edge_prior[label]`（占位 causal 1.0 / semantic 0.8 / temporal 0.6，标定流程调整）。MAGMA 的 φ 结构特征项在 1.0 以此落地，遍历完全由图结构信号驱动。
- **λ2·sim 方案 A**：锚点 = Stage 2 RRF 融合分，扩展节点 sim=0——真实 sim 在 Stage 3 不可得（向量在 ES，进图违反 M2 定案），方案 A 语义诚实、零新增参数；**方案 B（继承父节点 sim 衰减）留待效果验证后探索**，改动点只在 `_beam_search`。
- **实体顶点不作束搜索扩展路径**（无 φ、过不了粗筛），实体关系在收敛后以叶子节点 + entity 边统一挂回结果子图（s_effective=None，不参与硬过滤与排序权重）。
- **ρ 与 ES 的物理隔离靠签名**：`_stage3_traverse` 无 es/rho 入参；Stage 3 收敛后的第二次 θ 硬过滤放在 `retrieve()` 主函数（θ 需要 ρ，但 ρ 因此进不了遍历）。
- 占位参数全部集中 `RetrieveConfig`（frozen）：rrf_k=60、beam_width=8、max_depth=2（对齐 spike q1）、λ1=λ2=λ3=1.0、token_budget=2000 等；token 估算 len//4 占位，不引入 tokenizer；Stage 4"压缩"为前缀截断占位，正式叙事压缩语义待标定。

**踩坑（后续模块要沿用）**
- `n_star_cached=0` 的节点会被前置粗筛 `n_star_cached > n_now` 全灭——writer 已改为默认自动计算；任何手写图数据（测试夹具、迁移脚本）都必须给正确视界。
- 束搜索扩展脚本里 dst 侧属性用 `project + by(values(...))` 取；事件节点缺属性会整行丢失，所以 dst 过滤必须先锁 `node_type='event'`（实体顶点无 φ）。
- Gremlin 脚本里 `.in('label')`、`otherV()`、`P.gt((nNow as long))` 均可用（in 是 Groovy 保留字但 JanusGraph 脚本引擎兼容，M2 测试已在用）。

**遗留**
- Stage 4 的 relevance 归一化基准是候选池内 max path_score，跨查询不可比——标定阶段再定口径。
- λ2·sim 方案 B、边先验取值、束宽/深度、token 预算阈值全部待种子期效果验证后走标定流程。
- M4 图 `m4_*` 按红线 5 只 close 未 DROP，`make reset` 统一清。

## M5 — MCP / SDK 接口层（2026-08-05，本地 lint + 99 单测 + 50 集成全绿；CI 已确认全绿）

**交付**
- 新包 `services/api`（lethefield-api，框架无关核心 + 三个薄壳）：
  - `service.py`：四操作业务核心。record/flag_conflict 只到 EX ack（SS→RMS 异步入链是 M14/M15）；reinforce = 唯一同步直连旁路（ff.apply_reinforce）+ fire-and-forget 元事件（后台 daemon 线程，可注入同步实现）；retrieve 调 M4 引擎。
  - `ex_ingest.py`（**契约 1 生产侧雏形**）：`ex_{space_id}` keyspace（cassandra-ex，M1 物理隔离红线）+ 两表——experience_events（n 主键，Redis INCR `ex:n:{space}` 分配）/ meta_events（**不分配 n**，按 node_key 分区，count 字段为 M7 合并预留）；`n_now` Redis 缓存 + MAX(n) 重建。
  - `auth.py`（**契约 3 验证侧**）：claims `account_id/space_id[]/agent_actor_id/scope[]`，HS256 + env LETHEFIELD_JWT_SECRET（dev 默认密钥，M16 签发侧落地后轮换）；scope/space/debug 判定。
  - `errors.py`：错误码契约初版（8 个码 + HTTP 状态映射 + 统一结构）。
  - `http_app.py`（FastAPI 四端点 + Bearer + **限流挂载点** RateLimiter protocol/Noop 默认）、`sdk.py`（httpx 四方法，错误码映射异常）、`mcp_server.py`（mcp 1.x FastMCP stdio 四工具，token 走 env LETHEFIELD_MCP_TOKEN）。
- `libs/clients` 加 `ex_cassandra_cluster()`（EX 集群工厂与 cell 分开，防混用）。
- 单测 26 项（auth 判定矩阵/service 四操作/debug 裁剪/n 分配/keyspace 命名/MCP 注册）；集成 14 项（五条验收标准全覆盖 + SDK 走真实 uvicorn 回环）。

**关键决策（两处已升级确认）**
- **EX 摄入最小路径现在做**（不等 M10）：否则"同步等 EX ack"验收无法真实通过；任务划分 Phase 1 本就含"M10 的 EX 摄入路径"。边界：只做写入 + n 计数，Pulsar 发布/namespace/生命周期流水线留 M10。
- **FastAPI + MCP 官方 SDK + httpx + PyJWT**（备注留痕：未来按需求不排除手搓；业务核心在 service.py，换壳不动核心）。mcp 包锁 `<2`（2.0 移除 fastmcp 模块，API 不兼容）。
- **请求体携带 agent_actor_id → 400 actor_spoof**（fail-closed，不静默忽略——静默会让调用方误以为声明生效）。
- **FF 内部字段四操作统一 debug scope 才出**：retrieve 响应默认只有 node_key/content/tau/brief/edges；reinforce 响应默认 {node_key, applied}——防"经写接口旁路探测 FF 状态"绕过 retrieve 的 debug 管控。
- **图名 = space_id** 约定（M9 调度器落地后改查映射表）；space_id 命名约束 [a-z0-9_]≤40（EX keyspace 需要，fail-closed 不改写）。

**踩坑（后续模块要沿用）**
- **FastAPI 端点必须 sync def**：gremlin_python 同步客户端内部 run_until_complete，在 async 端点的事件循环里直接 RuntimeError；sync 端点由 FastAPI 放线程池执行。请求体注入用 `Annotated[dict, Body()]`（`Body(...)` 作默认值触发 ruff B008）。
- **PyJWT 的 required 校验参数名是 `require`（options 内）**，写 `required=` 会被静默吞掉不生效——必需 claim 改手动检查。
- **httpx.ASGITransport 只支持 AsyncClient**；同步 SDK 的集成测试走真实 uvicorn 回环（daemon 线程 + 空闲端口），比 ASGI transport 更贴近部署形态。
- mcp 包 2.0 移除了 `mcp.server.fastmcp`——锁定 `mcp>=1.2,<2`。

**遗留**
- reinforce 时间窗合并（M7 验收项）：M5 每笔一笔（count=1），合并器归 M7。
- record 后的 Pulsar 发布/SS→RMS 入链（M10/M14/M15）；n 分配与重建的并发竞态加固（M10）。
- JWT 签发/吊销/debug scope 签发管控（M16）；限流阈值标定（M12/标定流程）。
- MCP 说明书（引导 LLM 主动使用的 prompt 工程）按开发文档批注后期独立做。
- 集成测试 EX keyspace 用完 DROP（cassandra-ex 是我们的测试数据；图 `m5_*` 仍按红线 5 只 close）。

---

## M6 — FS sweep worker（2026-08-05，本地 lint + 145 单测 + 57 集成全绿；CI 已确认全绿）

**交付**
- 新包 `services/fs`（lethefield-fs）：`sweep.py`（单 space 一轮：忽视惩罚 → 固化判定 → 归档判定 → n_star 顺带刷新，固化节点整体跳过）、`archive.py`（独立流程一：快照=节点全字段+邻接 → archived_nodes → 删 ES 向量 → 热图移除）、`consolidate.py`（独立流程二：置 consolidated_at + n_star_cached=LONG_MAX，幂等不覆盖首次时间戳）、`worker.py`（list_spaces 逐 space + Redis 心跳 + fs_sweep_processed_total/fs_sweep_lag_seconds 指标，--once 单轮）、`liveness.py`（Dead Man's Switch 巡检，超窗口退出码 1）。
- `libs/clients`：`ex_n.py`（n_now/keyspace 命名/Redis 键约定单点，ex_ingest 薄委托——FS 需要 n_now 而服务间禁止互相 import）；`archive.py`（ARCHIVE_TABLE + CQL 封装，FS 不裸写 CQL）；`ControlPlaneStore.list_spaces()` + 过渡实现 `ExKeyspaceControlPlaneStore`（EX keyspace 元数据推导，M9 切映射表零改动）。
- `ff.py`：`FFConfig.grace_n`（占位 40）；纯函数 `archive_eligible`（n_now ≥ n_star_cached + grace_n，M7 重建必须复用）；`compute_delta` 纯决策（固化锁定分支抽出可单测）；`read_phi` 增读 consolidated_at。
- schema 升 **17 键**（`consolidated_at` Date）；`retrieve.py` 两处 θ 硬过滤对固化节点旁路（s_effective 取 s 现值、跳过衰减现算），NodeProps/ScoredNode 加 consolidated 标记；`vectors.py` 加 `delete_vector`。
- 单测 +46（ex_n/archive/list_spaces/ff 固化分支与 archive_eligible/fs 判定矩阵与 liveness）；集成 `tests/integration/test_m6_fs.py` 7 项（忽视区间幂等、顺带刷新、归档端到端含快照与 ES 清理、宽限期内 reinforce 自取消、固化端到端含检索 θ 旁路与 δ 锁定、元事件不推进 n、liveness 退出码）。

**关键决策（四处设计未覆盖，均已升级确认入档开发文档 M2/M4/M6/§20）**
- space 枚举走 ControlPlaneStore 抽象，过渡期读 EX 集群元数据按 `ex_{space_id}` 约定推导（只读控制面元数据不违反红线 1）；否决 Redis 活跃集合（改 M5 冻结契约 + 会漂移的第二真相源）。
- 固化标记 = `consolidated_at`（timestamp）而非 Boolean——白得审计时间戳，Boolean 能做的它全能做。
- 归档载体 = 本 space RMS keyspace 内 `archived_nodes` 表（销毁随 keyspace DROP 自动完成、迁移 snapshot 自动携带）；否决共享 rms_archive（销毁退化为 range delete）与 EX 集群归档表（污染 source-of-truth）。
- 宽限期 = 事件距离 grace_n（记忆动力学只在 n 域运算；静默 space 不该被日历惩罚）；固化判定先于归档（固化是对抗遗忘的保护）。

**踩坑（后续模块要沿用）**
- Gremlin 投影取"属性存在性"用 `values('k').fold()`，**不要** `properties('k').fold()`——VertexProperty 在 GraphBinary 里是 custom 类型，gremlin_python 反序列化直接 KeyError。
- gremlin_python 反序列化的 Date 是 **naive datetime**（UTC 语义）：`.timestamp()` 前必须 `replace(tzinfo=UTC)`，否则按本地时间解释（归档快照 tau 偏差 8 小时实测）。
- 全量套件变慢/超时的最大嫌疑是 **Cassandra schema 堆积**：测试图只 close 不 DROP，keyspace/table 越攒越多（实测 51 keyspace / 463 表时 schema 操作 >12s，图创建连锁超时）——先 `make reset` 再怀疑代码。
- bind mount 内容变更不触发 compose recreate，`docker compose restart <svc>` 才重读；JanusGraph 容器重建会撞 "same instance id already open"（旧注册心跳未过期），已加 `graph.replace-instance-if-exists=true`；`evaluationTimeout` 放宽至 180s（冷图创建在全量负载下超默认 30s）。

**遗留**
- sweep 全量取回单 space 事件节点（1.0 规模从简）；分页/批处理与冷热分层节奏待 M9 tier 落地。
- 归档快照 tau 以 epoch 毫秒落 JSON，重建解析约定随 M7 重建脚本定。
- M6 图 `m6_*` 按红线 5 只 close 未 DROP，`make reset` 统一清。

---

**下一步：M7 纠错机制（supersedes）**（开发文档 §8）。要点：纠错事件化走完整 EX→SS→RMS 入链；consolidation worker 建 supersedes 边 + 异步 −0.5（同一对节点幂等）；reinforce 时间窗合并（M5 每笔一笔 count=1，合并器归 M7）；重放重建脚本必须复用 `ff.archive_eligible` 与 `libs/clients/archive.py` 访问层，并覆盖 archived_nodes 表重建（归档判定从 EX 事件流确定性重推）。

---

## M7 — 纠错机制（supersedes）（2026-08-06，本地 lint + 169 单测 + 62 集成全绿）

**交付**
- `libs/clients/ex_n.py` 升级为 EX 读访问层：`META_TABLE` 单点（ex_ingest 改 import 并 re-export）、`ExEvent`/`MetaEvent` 数据类、`list_experience_events`（按 n 升序）/`list_meta_events`（可按 node_key 过滤）——消费方不裸写 CQL，与 archive.py 同规约。
- `services/api/ex_ingest.py`：reinforce **时间窗合并**（`REINFORCE_MERGE_WINDOW_MS` 占位 60s）——窗口内同节点多次强化同主键 UPDATE（count 累加、n_at_event 刷新，不产生新行）；service.reinforce 传入窗口参数，接口形状不变。
- `services/rms/corrections.py`（新）：纠错处理器（consolidation 纠错职责的最小独立形态）——扫 EX `ref_conflict` 事件 → **单 tx 原子幂等脚本**（tx 内查边：已存在 → `duplicate` 零写入；否则建 supersedes 边 + 落 `ff.compute_delta` 预算的 −0.5）。新节点按 `ref_ex=event_id` 反查、旧节点按 `node_key=ref_conflict`；任一缺失 → pending 下轮再处理。CLI `python -m lethefield_rms.corrections [--space S]`。
- `services/rms/rebuild.py`（新）：EX 重放重建——纯重放模型（`replay_events`，不触存储可单测）+ 执行器 + CLI `python -m lethefield_rms.rebuild <space> [--target-gname G]`。重放：经验事件 n 序建节点 + temporal 边 n 序链；`ref_conflict` → supersedes 边 + conflict δ（同对幂等）；reinforce 元事件按 count 展开；忽视/固化/归档**理想化 sweep 确定性重推**（每个事件推进点判定，复用 ff 单点）。归档快照沿用 M6 build_snapshot 格式、经 libs/clients archive 访问层落 `archived_nodes`。
- `ff.py`：`neglect_due`/`consolidate_due` 纯判定从 fs/sweep **迁入 ff 单点**（sweep.py re-export 保持兼容）——重放重建与生产 sweep 共用同一份，禁止两处副本。
- `schema.py`：`FORBIDDEN_FLAG_TOKENS` + `find_invalidation_flags`（M7 红线：禁硬失效标志）；`scripts/check_rms_schema.py` 第 5 层巡检——真实图全量元素名扫描。
- 单测 +24（ex_n 读访问/合并矩阵/corrections 决策/rebuild 重放模型 9 场景/schema 红线）；集成 `tests/integration/test_m7_corrections.py` 5 项（五条验收标准全覆盖）。

**关键决策（s 来源已升级确认入档开发文档 §8 两档验收 + M14 scoring_result 定案）**
- **初始 s 走注入 `s_resolver`**（默认占位常数 1.0）：LLM 打分不可重放，EX 无 s 字段是保真缺口；M14 落地时打分结果以 `scoring_result` 元事件回写 EX（不推进 n），resolver 切换真实来源、验收升级全保真。接口形状先行，过渡自动收口。
- **`node_key_of(event_id)` 过渡约定**（同款模式）：EX 不存 node_key（M15 写入链才定生成规则），重建节点键由 rebuild 单点函数生成，M15 落地后对齐切换。
- **理想化 sweep 语义**：重放 = 规范历史（假定 sweep 每事件推进点执行，区间幂等序列恰给 floor(Δn/N) 次惩罚），不是逐拍复刻真实 sweep 节奏——后者本就不可复现。
- **边即幂等标记**：同对节点幂等靠 tx 内查 supersedes 边，无额外状态表；不同新节点纠同一旧节点 = 两条边两次 −0.5（幂等约束的是"同一对"）。
- 检索侧零改动：supersedes 重定向 + trace_history 在 M4 已实现（签名隔离验证通过），M7 只补验收测试。

**踩坑（后续模块要沿用）**
- 重放重建的边只能建**双端都在热图**的：归档节点不落图，带归档端点的 temporal/supersedes 边 addEdge 直接失败；归档快照的邻接同样只含对端仍在热图的边（顶点归档即携边移除）。
- 重建图顶点的 `space_id` = 源 space（图名 ≠ space_id），校验查询的 sid 绑定别随手写 gname（集成测试踩了一次）。
- Cassandra UPDATE 同主键 = upsert：reinforce 合并不改契约 1 表结构、不产生新行，`count`/`n_at_event` 原地刷新。

**遗留**
- corrections 是单轮形态（常驻循环/心跳留待 M15 写入链合并统一落地）。
- rebuild 不重建 semantic/causal/entity 边（consolidation 推断产物，EX 无来源，M14/M15 后补齐）；`consolidated_at` 时间戳不可复现（存在性保真、时间戳置执行时刻）。
- `REINFORCE_MERGE_WINDOW_MS` 60s 与 `PLACEHOLDER_S` 1.0 均为占位，标定流程调整。
- 重建图与原图并存的切换/校验流程（"删了重建"演练编排）归 M10 混沌演练。
- M7 图 `m7_*` 按红线 5 只 close 未 DROP，`make reset` 统一清。

---

**下一步：M8 记忆空间模型与鉴权（space_id）**（开发文档 §9）。要点：顶层分区键从 agent_id 改为 space_id 的模型落地；EX/Pulsar 侧分区键验收项与 C 协作；space_id 字符集约束 M8 正式定义（现行为 libs/clients ex_n 的 fail-closed [a-z0-9_]≤40）。

---

## M8 — 记忆空间模型与鉴权（space_id）（2026-08-06，本地 lint + 单测 + 集成全绿）

**交付**
- `libs/clients/spaces.py`（新）：space_id 字符集 **[a-z0-9_]≤40 正式定案**单点（`validate_space_id` / `SPACE_ID_MAX_LEN`；M5 起 ex_n 的 fail-closed 行为转正，已建 space 零迁移）+ `SpaceType` 枚举（companion/project）。`ex_n.keyspace_name` 改为委托，行为不变。
- `control_plane.py`：`SpaceMapping.space_type` 可选注解字段（默认 None，向后兼容 M0 冻结接口；`update_space_status` 透传）——仅产品/运营标注，M9 映射表沿用。
- `services/api/service.py`：四操作入口 `_require_valid_space`（`require_space` 后），非法 space_id 400 `bad_request` 零副作用，不直达图名/keyspace 命名路径。
- `scripts/check_space_model.py`（新）：静态巡检双红线——全库无独立词 agent_id 残留、`space_type` 只允许出现在 libs/clients（核心服务禁分支）；不起栈，已接 `scripts/ci.sh`。
- 单测 +17（spaces 字符集矩阵/SpaceType、control_plane space_type 往返、service 四操作 fail-closed）；集成 `tests/integration/test_m8_space_model.py` 5 项（多写入者共享 EX/RMS + 来源过滤 + 非法 space_id 零副作用三形态）。

**关键决策**
- **字符集维持 [a-z0-9_]≤40 不变**：与 Cassandra keyspace / JanusGraph 图名 / ES routing / Pulsar namespace 全兼容，且已建 space 零迁移；开发文档 §9 把正式定义权交给 M8，维持现状即定案。
- **校验单点放 spaces.py 而非 ex_n**：字符集是跨域命名约束（四种存储共用），ex_n 只是 EX 序号语义；ex_n.keyspace_name 薄委托（与"共享代码只允许 libs/ 三样"不冲突——spaces.py 属 libs/clients）。
- **space_type 落点 = SpaceMapping 注解字段**：space 元数据的唯一模型点，M9 映射表落地时零改动沿用；无写入入口（开通流程属 M9/M10）。
- **Pulsar 侧分区键验收挂起**：Pulsar 未入仓（M10），开发文档 §9 验收第 1 条标注"Cassandra/ES 已验证，Pulsar 随 M10 与 C 协作"。

**踩坑**
- 巡检脚本自身与测试 docstring 必然提及被巡检的词（agent_id/space_type）——脚本自我豁免路径，docstring 改写措辞（"agent 级分区键"/"空间类型分支"）保持扫描严格。
- **`LETHEFIELD_JWT_SECRET` 是进程级共享 env，集成测试模块不能在 import 时设定各自不同的值**——pytest 先导入全部模块再执行，最后导入者的值覆盖前者，先执行模块的 token 全部 401（M8 import 时设 m8 密钥 → M5 全套 12 项 401 连锁失败）。定案：密钥在模块 fixture 里设定（本模块执行窗口内生效），import 时只定义 TEST_SECRET 常量。后续带 JWT 的集成测试模块沿用此模式。
- Cassandra 累积 keyspace/表过多会拖垮 schema 操作（本轮回归 q3 建图 12s CQL 超时，nodetool 查询挂起）——`make reset` 清卷后恢复，与 AGENTS.md 既有结论一致。

**遗留**
- JWT claim 里的 space_id 值不做字符集校验（签发属 M16；入口 `_require_valid_space` 已兜住请求路径）。
- M8 图 `m8_*` 按红线 5 只 close 未 DROP，`make reset` 统一清。

---

**下一步：M9 Cell 架构 + 租户调度器**（开发文档 §10）。要点：space→Cell 映射表正式实现（替换过渡期 ExKeyspaceControlPlaneStore，list_spaces 调用方零改动）；映射表备份与导出是 1.0 验收硬指标（映射表丢失 = 全部 space 失联）；计算池持映射缓存直连 Cell——调度器宕机只影响开通/迁移/注销，不影响存量读写；注销硬性顺序：计算池驱逐图实例先于任何 DROP KEYSPACE（红线 5）；不做自动再平衡/动态打分装箱（设计已否决）。

---

## M9 — Cell 架构 + 租户调度器（2026-08-06，本地 CI 全绿：lint + 单测 225 + 集成 73）

**交付**
- `libs/clients/control_plane.py`：**`MappingTableControlPlaneStore` 正式实现**——映射表 =
  `lethefield_control` keyspace（cassandra-cell 上专用 keyspace，spaces/cells 两表，直写 CQL 不经
  JanusGraph）；`list_spaces` 按 status=active 过滤（M6 已知过渡偏差"destroying 被扫入"收敛）。
  具体类扩展方法（不动 M0 冻结 ABC）：`ensure_tables`/`register_cell`/`update_cell_watermark`/
  `unregister_space`/`list_space_mappings`。过渡期 `ExKeyspaceControlPlaneStore` 整体删除，
  `StaticControlPlaneStore` 保留（单测夹具）；`local_cell()` 单点给出本地 Cell（endpoints 为
  JG 容器视角服务名，建图配置直接用）。
- `libs/clients/mapping_cache.py`（新）：`MappingCache`——计算侧 TTL 缓存（含负缓存），
  **控制面异常时服务陈旧值**（不刷新 TTL，故障期一直可用；无陈旧值才上抛）；写操作永远直达
  store。这是"调度器宕机不影响存量读写"（§17.2 硬性验收）的实现点。
- `libs/clients/control_backup.py`（新）：映射表 JSONL 全量导出/恢复（含非 active 映射，
  `"v": 1` 版本字段，restore 覆盖写幂等）——1.0 验收硬指标。
- `ex_n.py`：`ensure_ex_keyspace` DDL 从 ex_ingest 迁入（EX 命名/DDL 单点；调度器开通
  流水线 EX 步不依赖 API 服务），ex_ingest 委托 + re-export，调用方零改动。
- `services/scheduler`（新服务 lethefield-scheduler）：
  - `provision.py`：开通五步走——`select_cell`（open 中最大维水位最低，无 open 零副作用
    抛 NoOpenCellError）→ EX keyspace → Pulsar namespace → RMS 建图+schema（backend props 按
    Cell endpoints 推导，`schema.backend_props_of` + `ensure_graph_schema` 第三参注入）→
    **先存储后注册**；任一步失败逆序回滚，注册未执行；回滚本身失败抛 ProvisionRollbackError
    携带残留清单（不静默）。hot/premium tier 注册后预 open 图实例。
  - `destroy.py`：注销五步严格按序——标 destroying（缓存失效）→ **close +
    removeConfiguration 驱逐计算实例（红线 5，先于任何 DROP）** → rms_vectors 文档 → DROP 图
    keyspace → 删 namespace → 最后 DROP EX keyspace → `broadcast_destroy` 注入点（默认结构化
    日志，M10 接训练管线真接口）→ 清映射 + 全链路无残留校验（残留抛 DestroyError）。
    **eviction 语义：close 容错（幂等）、removeConfiguration 严格**——驱逐不完整直接中止，
    绝不带开着的图实例进 DROP。
  - `watermark.py`：`state_of` 纯函数（0.7/0.9 初值，SchedulerConfig）+ 注入式 `refresh_cell`
    （keyspaces/ES 分片实测，disk/heap 单节点无探针置 0，模拟负载走注入）。
  - `pulsar_admin.py`：tenant/namespace admin REST（tenant 固定 `lethefield`，namespace =
    space_id，M8 字符集四处共用）。
  - CLI `python -m lethefield_scheduler`：bootstrap / provision / destroy / watermark /
    export / restore / list。
- 调用方切换：`service.ApiContext` 增 `mapping_cache`（必填）+ 控制面独立 Cluster 连接
  （`control_cluster`，故障演练可与数据面隔离）；四操作经 `_resolve_gname` 解析，未注册
  space **404 not_found fail-closed**（`gname_of` 直连快路径消除）。FS worker / corrections
  枚举源切映射表（调用接口零改动）。
- 单测 +44（缓存 7 / 备份 4 / 映射 serde 1 / 水位 6 / 开通 9 / 注销 7 + API 未开通 404 等）；
  集成 `tests/integration/test_m9_cell_scheduler.py` 6 项（五条验收全覆盖：备份恢复演练 /
  开通三向映射 + 回滚 / 注销无残留 / 控制面真实 shutdown 后存量读写 / 水位转换闸门）。

**关键决策**
- **映射表物理位置 = cassandra-cell 专用 keyspace**：设计 §17.2"独立小集群或 Cell-1 专用
  keyspace"的最小部署形态；与图 keyspace 物理分离，红线 5 不涉及（非 JanusGraph 使用）。
- **图名约定不变（= space_id），改的是解析路径**：映射解析是"已开通且归属已知 Cell"的
  存在性闸门；多 Cell 形态下 cell_id 决定连接路由，本地单 Cell 形态下是 fail-closed 闸门。
  record 打未开通 space 从 Cassandra 报错变 404，严格改善。
- **space_type 不进调度器接口**：M8 巡检红线（只允许 libs/clients 出现），provision 只收
  tier；space_type 维持"无写入入口"现状。
- **跨 Cell 迁移不做**：迁移演练是 M10 验收项，本地单 Cell 无法演练；自动再平衡/动态
  打分装箱设计已否决。
- **Cell endpoints 语义 = JG 容器视角**：建图配置直接用（cassandra-cell/es-graph 服务名）；
  host 侧访问仍走 factories env。

**踩坑（后续模块要沿用）**
- **colima VM 内存不足会被内核 OOM killer 杀 JanusGraph**（exit 137，dmesg 可见
  "Out of memory: Killed process (java)"）——表现为 gremlin 连接拒绝、m6 起后续模块连锁
  ERROR、q1/q3/q4 基线假败。宿主机 16GB 下 VM 已从 8GiB 调到 **10GiB**；先查 dmesg 再怀疑代码。
- **套件负载下 Cassandra schema 变更（DROP KEYSPACE）可超默认 10s 客户端超时**——表现
  为调度器 destroy 间歇性 OperationTimedOut、且超时后服务端可能已部分应用（残留状态难复现）。
  调度器 DDL 统一 `DDL_TIMEOUT_SECONDS=60`（config 单点）。
- `pytest.raises(Exception)` 触发 ruff B017——集成测试断言"控制面不可达"用具体类型
  （NoHostAvailable/RuntimeError 元组）。
- 集成测试夹具中断会留下 active 映射+全套存储（污染后续用例的 list_spaces/备份内容）——
  开通后必须 try/finally 收 destroy（destroy 本身幂等 tolerate SpaceNotFoundError）。
- fake session 的 `execute` 签名要留 `**kwargs`——生产代码加 timeout 参数时不会连环炸。

**遗留**
- `broadcast_destroy` 是注入点（默认结构化日志）——M10 验收"训练管线销毁指令不是留空
  占位"届时接 M11 真接口。
- disk/heap 水位维度无探针（置 0），阈值 0.7/0.9 与容量上限（1500/3000）均为初值待标定。
- 调度器仅 CLI 形态（§17.7 明确不含 API 详定义）；多副本无状态部署属生产化课题。
- Pulsar namespace 配额/策略未配置（M10 Dead Man's Switch 一并落地）。
- 映射缓存 TTL 300s 初值；多 Cell 连接路由（按 cell_id 分连接池）留待多 Cell 形态。

---

**下一步：M10 EX 存储与 Pulsar 归属 + 三存储生命周期流水线**（开发文档 §11）。要点：
EX 集群池与 Cell 物理独立的验收巡检；开通/迁移/注销流水线终版（M9 已落地开通/注销，
M10 补跨 Cell 迁移演练并实测只读窗口 <1 分钟）；训练管线销毁广播接真实接口（M9 注入点）；
EX 摄入 Dead Man's Switch（Pulsar consumer ack + backlog 监控）；EX 高密度低规格机型与
封存层优先封存 EX 的成本缓解落地。

---

## M10 — EX 存储与 Pulsar 归属 + 三存储生命周期流水线（2026-08-07）

**升级确认定案（参谋会话，三个分支）**
1. 销毁广播 = 训练 tenant 持久化控制 topic（契约 5 冻结最小字段集：space_ref 哈希 /
   指令类型 / 发起者 / 时间戳 / 注销工单引用，M11 只能向后兼容扩展）。三条硬约束：
   生产者等 broker ack、最终失败禁止静默进第 5 步；控制 topic consumer backlog 纳入
   监控告警；schema 单点定义。
2. 迁移演练两档：本地档 = RMS 真跨 Cell（compose cell2 profile 按需起，heap ~1.3G
   上限写注释防调大）+ EX 同集群 keyspace 复制全步骤真实执行；准出档（EX 跨集群
   sstableloader）临时扩容补跑，登记为已知缺口。演练测试探测 profile 栈不在线则
   skip + 提示，不阻塞常规 CI。
3. DMS 双路、监控信号与数据面物理分离：管道活性探针（page 级，监控 tenant 专用
   probe topic，禁止向 space namespace 发探针）+ space 写入新鲜度（observation 级，
   活跃 = 曾成功摄入或 hot/premium 全集，超窗无新事件即告警；W 与阈值进配置待标定）。

**交付**
- `libs/clients/training_control.py`（契约 5 单点）：`SpaceDestroyCommand` schema
  （含 `"v": 1`）、`space_ref_of`（sha256 域分隔不透明哈希，M11 样本 schema 复用）、
  训练 tenant/topic 命名常量。销毁广播生产者 `scheduler/destroy_broadcast.py`
  （同步等 broker ack、有限重试、最终失败抛 `BroadcastError`）；destroy 第 4 步
  默认即真实链路——广播失败 → 告警 LogEvent + 留痕并中止，**不进第 5 步**
  （映射留 destroying，步骤 1–3 幂等可重试）；无 Pulsar 通道直接失败（禁止留空
  占位）。最小接收 consumer `scheduler/training_control_sink.py`（durable 订阅
  `training-destroy-sink`，接收记录落决策留痕事件；M11 worker 复用同一 subscription
  继承积压，接口零变更）。bootstrap 备妥训练 tenant/namespace + 审计级 retention。
- Pulsar namespace 配额/策略落地（v0.7 选型核心理由 + M9 遗留）：provision 建
  namespace 时配 retention（7d/2GB）+ backlog quota（512MB，producer_request_hold——
  背压对生产者明确可见）；`pulsar_admin` 增 set_retention/set_backlog_quota/topic_stats。
- EX 摄入 DMS：`ops/ingest_dms`（对齐 clock_monitor 模式，`--once` 单轮退出码语义）。
  ① 探针：监控 tenant `lethefield-monitoring/ops/ingest-probe` 真实收发 ack，失败即
  page 级；② 训练控制 backlog：持续非零超窗 → page 级（consumer 停摆 = 销毁指令
  静默积压 = 合规失效），观测值报 `lethefield_pulsar_backlog_events{namespace_class}`
  gauge；③ 写入新鲜度：摄入路径在每次成功写入后维护 Redis `ex:last_write:{space}`
  （ex_n 单点），活跃集合 stale 翻转边告警（持续不重复刷，恢复产 recovered 事件）。
  阈值全部进 `DmsConfig`（env 可配占位，禁硬编码）。
- 跨 Cell 迁移流水线 `scheduler/migrate.py` + CLI `migrate`：标 migrating（API 写路径
  record/flag_conflict/reinforce 429 rate_limited——复用现有契约码，retrieve 只读
  放行）→ RMS 目标 Cell 重建（建图+schema → M7 EX 重放 → ES 向量应用层复制）→
  EX keyspace 复制（本地档 scratch 过渡全步骤）→ 等价校验 → 切映射
  （`update_space_cell` 新方法）→ status 回 active → 源侧清理（红线 5 顺序）。
  失败语义：切映射前回滚目标侧 + status 复原；切映射后残留抛 MigrationCleanupError
  不静默。**等价校验对齐"目标图 == EX 重放计划"**（不是与源图比——源图可能含重放
  不覆盖的 consolidation 推断边，且窗口在途写入只反映在重放里；EX 是唯一 SoT）。
- compose `cell2` profile（cassandra-cell-2:9142 / es-graph-2:9300 / janusgraph-2:8183，
  heap 合计 ~1.3G 注释锁死）；JG-2 用 cell2 变体 properties（指向 cell2 后端）。
- 集成演练 `test_m10_migration_drill.py`（profile 不在线自动 skip + 提示）：端到端
  开通 → 造数（事件/纠错/reinforce/向量，源图 M7 重放构建）→ 迁移 → 窗口内
  record 429 + retrieve 可读 → 目标侧等价（图计数/向量/kNN/EX 行数）→ 源侧无残留 →
  **实测只读窗口 15.6s < 60s 预算**（rms_rebuild 11.7s 为最大头，冷图创建）→
  记录落 `deploy/baselines/m10_migration_drill.jsonl` → cell2 客户端 destroy 清理。
- 单测 +45（契约 5 serde/哈希、广播重试与失败中止、sink 接收记录、迁移编排/回滚/
  清理残留、EX 复制全步骤、API migrating 429 三写 + retrieve 放行、DMS 三路 18 项）；
  集成 +8（广播链路/失败重试/tenant 隔离 retention、DMS 三路、迁移演练）。

**踩坑（后续模块要沿用）**
- pulsar-client 的 receive 参数名是 `timeout_millis`，写错成 `timeout_ms` 会被宽泛
  except 吞成"无消息"假阴性——sink 只捕 `pulsar.Timeout`，其他异常上抛。
- Pulsar 412：backlog quota 必须 < retention size，配置初值要成对调。
- **gremlin_python 客户端基于 tornado 单连接，跨线程共享会死锁**（迁移线程 + API
  探测线程各用各的连接——演练首跑挂 600s 的根因）。
- 演练里向迁移窗口注并发写不可控（翻转/重放/计数的交错），窗口语义探测必须确定性：
  等控制面读到 migrating 再断言 429，迁移前/后各验一笔真实写入。
- EX keyspace 命名契约下"同集群迁移"最终态只能靠 scratch 过渡（copy → 校验 → DROP 源
  → 回拷正名 → DROP scratch），Cassandra 无 keyspace rename。

**遗留 / 已登记缺口**
- EX 跨集群 sstableloader 流式传输（准出档，临时扩容环境补跑；`_copy_ex_keyspace`
  的源/目标 session 参数化即该形态的同一接口）。
- API 层多 Cell 连接路由（按 cell_id 分连接池）仍是生产化课题——演练经 cell2 直连
  客户端校验 RMS 等价，不经 API 读 RMS；多进程 MappingCache 的状态翻转传播靠 TTL
  （跨进程 invalidate 不存在）。
- DMS 阈值（W/stale/backlog 窗口）与 Pulsar 配额初值均待标定；封存层 EX 先行的阈值
  与恢复流程对齐 §15.2 待细化（课题待决项 2）。
- make_broadcast 的 producer 随闭包持有不显式关闭（CLI 进程退出回收；常驻形态 M11
  worker 接管时一并处理）。

**下一步：M11 训练数据管线（1.0 最小实现）**（开发文档 §12）。四入料口、R1–R3 高价值
时刻判定、授权注册表拦截（③④ 类数据入 topic 前拒绝）、可撤回性（space_ref 成组定位
O(清单)——契约 5 的 `space_ref_of` 单点直接复用）、销毁指令真实消费（M10 sink 的
subscription `training-destroy-sink` 继承积压）。

## M11 — 训练数据管线（1.0 最小实现）（2026-08-08，本地 lint + 单测 307 + 集成 88 绿 1 跳过）

**交付**
- `libs/clients/training_feed.py`（feed 信封单点）：topic `lethefield-training/feeds/raw`
  （短 retention 7d 占位，过境 ≠ 沉淀）；`FeedEvent`（kind 四值 × source 配对校验，
  serde fail-closed）；R1/R2 判定纯函数 `decision_rules` 单点（提交路径与 worker 共用）；
  `make_feed_publisher`（同步等 ack）。`DESTROY_SUBSCRIPTION` 单点上移
  `training_control.py`（M10 sink 与 M11 worker 共用继承积压）。
- 授权注册表 store 下沉 `libs/clients/auth_registry.py`（§12.4 待决项 1 按现状收口 =
  独立 Postgres 表；共享代码只允许 libs/，API 闸门/worker/ex-feed 三处共用），新增
  `delete`；ops/auth_registry 改薄 re-export，CLI 零改动。decision_log 补 §11.3 三列
  （agent_suggestion/outcome/escalation_type；init.sql + 幂等迁移
  `deploy/postgres/migrations/001_decision_log_m11.sql`）。
- 四入料口：① decision_log submit 路径 R1（outcome≠accepted）/R2（escalation_type 非空）
  判定命中才发布（best-effort，不阻塞留痕提交；① 类不过授权闸门）；② `submit-incident`
  CLI（人工提交即触发，rule=R5）；③ API retrieve 发射最小化召回明细（space_ref +
  node_key 列表 + θ 阶段计数 + query 类别，无 query 原文——θ 数据源 =
  `RetrievalResult.stats`，四阶段签名不动），LogEvent 恒发 + CALIBRATION 闸门后入
  feed topic；④ `ex_feed` 只读 EX 派生纠错对（旧内容按 ref_conflict 反查 EX，不触
  RMS），CONTENT_COPY 闸门入 topic 前拒发，state 文件幂等。
- `services/training`（lethefield-training）：样本 schema（§12.4 统一格式 + scrubbed）、
  热层本地目录（hot/samples-日期.jsonl + index/{space_ref}.jsonl 清单索引，`scrub`
  只读目标清单按单重写 = O(清单)，内容字段清空骨架保留）、R3 召回窗
  （recall_window.jsonl 重启可重建，W_r3 默认 24h 占位 env 可配）、worker 双订阅轮询
  （feed `training-sample-worker` + control `training-destroy-sink`；WorkerRuntime
  常驻订阅 + `_subscribe_with_retry`），worker 侧授权复查第二道防线；销毁处置 =
  scrub + 注册表项删除 + LogEvent 处置留痕。指标
  `lethefield_training_samples_total{source,rule,review_status}` /
  `lethefield_training_feed_dropped_total{kind,reason}`（白名单新增四个低基数枚举标签）。
- 验收全过：③ 闸门授权切换前后对比、worker 侧未授权丢弃、R1/R2/R3/R5 各有触发场景、
  常规流量零样本、撤回 scrub O(清单) 骨架保留、销毁广播真实消费 + 注册表删除 +
  处置留痕、worker 不查业务库静态测试（ast 扫 import）。
- 单测 +37（feed serde/判定规则、样本 serde/scrub、热层清单/scrub/prune、召回窗、
  worker 路由全 kind、ex-feed 派生/幂等/闸门、无业务库静态扫描）；集成 +8
  （`test_m11_training_pipeline.py`）。

**升级确认（参谋会话定案，已入档开发文档）**
- R3 检测机制 = 关联式（召回明细过境 topic + node_key×W_r3 窗关联，命中才产样本；
  未经召回的纠错不计入 R3，归 R4/R6 再议）；否决"纠错即样本"（污染标定闭环）。
- decision_log 扩三列（补齐 §11.3 既定要求，非新增需求）；否决"只进训练样本不动表"
  （留痕库审计面缺位 + 数据分裂）。

**踩坑（后续模块要沿用）**
- Pulsar Exclusive 订阅在前一 consumer 关闭后短暂窗口内报 ConsumerBusy（broker 连接
  回收滞后）——测试里快速重订阅全灭过一次；常驻形态订阅常开（WorkerRuntime），
  重订阅必须退避重试。
- `make reset` 后 Pulsar 是全新卷：训练 tenant 的 control/feeds namespace 都不存在，
  集成 fixture 必须自建（M10/M11 fixture 同款模式）；scheduler bootstrap 已含两个
  namespace 的 ensure + retention。
- M10 的"训练 tenant 只有 control namespace"断言随 M11 feeds namespace 失效——
  改为子集断言（合法集合 = control + feeds，模块执行顺序不定）。

**遗留 / 已登记缺口**
- ③ 口授权闸门 1.0 在 API 进程内执行（开发文档写"过滤器读日志管线"，日志管线 M12
  才建）——M12 上线后过滤器改读管线，进程内闸门可退役；召回明细 LogEvent 目前只到
  stderr（es-ops 写入路径同属 M12）。
- W_r3（24h 占位）、feeds retention（7d 占位）待标定（§20）。
- 授权判定每请求一次 PG 查询（1.0 从简；高 QPS 场景加 TTL 缓存，注意撤回生效延迟语义）。
- R4–R6、模型预标注、温/冷分层自动化、Parquet 冷层 = 既定延后项。

**下一步：M12 可观测性埋点（开发期最小集）**（开发文档 §13）。

## M12 — 可观测性埋点（开发期最小集）（2026-08-11，本地 lint + 单测 326 + 集成 94 绿 1 跳过）

**交付**
- 日志管线落地：`libs/logschema/es_sink.py`（唯一写入端）——`emit(event, sync=)` stderr
  恒留底 + configure 后异步批量 bulk 进 es-ops（`lethefield-logs-YYYY.MM.DD`）；一次性
  CLI 用 sync=True（惰性建 shipper 同步直写防退出丢尾部）；ES 不可达 fail-open。
  既有 13 处 print(LogEvent) 发射点全部收编（api/fs/training/ingest_dms/scheduler/
  decision_log/clock_monitor/sink）。
- 暴露口：API `/metrics`（FastAPI sync GET，不挂业务 scope——运维通道 1.0 内网口径）；
  worker 进程统一 `lethefield_metrics.start_metrics_server`（端口 fs 9101 / training 9102 /
  ingest_dms 9103 / exporter 9104，env LETHEFIELD_METRICS_PORT，0=不起，--once 不起）。
- 请求路径指标：record_confirm / ex_write / retrieve_stage_duration{stage=knn,subgraph,
  ff_filter}（retrieve 主函数三段计时，stage3 签名纪律不破）/ ff_theta_filter_ratio
  （pool=0 跳过）；reinforce 增发 `memory_reinforced` 明细事件（touched 侧数据源）。
- 离线聚合 `ops/metrics_exporter`（loop + --once + :9104；红线 1 边界 = es-ops 日志流 +
  system.size_estimates + 映射表 + rms_vectors 元数据）：留痕线两 counter（has_agent_suggestion
  才入分母）、ff_delta_applied_total（日志流折叠，一次性 CLI 的统一通道）、
  graph_open_duration_seconds{type}（显式 open 点 LogEvent 折叠成 histogram）、
  ff_recalled_then_touched_rate（召回×reinforce 窗关联）、graph_lru_cache_hit_ratio
  （代理口径）、cell_watermark_ratio（命名规则强制 _ratio）、space_storage_bytes{tier}
  （size_estimates + ES 文档数比例分摊近似）。
- ingest_dms 顺带发射 `lethefield_ex_last_write_age_seconds{dimension=max/p95}`。
- ③ 收口（升级定案 + M12 验收项）：`lethefield_training.recall_filter`（es-ops →
  CALIBRATION 闸门 → feeds/raw，checkpoint at-least-once）；召回明细带 event_id +
  stage_ms；worker `RecallWindow.mark_seen` 按 ID 去重（缺 event_id fail-closed）；
  **API 训练 topic 发布代码整体删除**（ApiContext 的 auth_registry/feed_publisher/
  _make_lazy_feed_publisher 全移除）；M11 集成测试改写为定案形态。
- compose 新增 prometheus(9090) + grafana(3000)（deploy/prometheus/prometheus.yml 五个
  host.docker.internal scrape job；Grafana provisioning datasource + 三线六面板 dashboard）。
- 单测 +17（shipper 批刷/兜底/sync、exposition、exporter 聚合纯函数四组、recall_filter
  三态、worker event_id 去重/fail-closed）；集成 +6（`test_m12_observability.py`：
  shipper→es-ops 可搜、API /metrics 四指标族、exporter 全序列、③ 收口链路端到端、
  Prometheus /api/v1/query 实查 + Grafana datasource、DMS 龄期 gauge）。

**升级确认（参谋会话定案，已入档开发文档 M12）**
- graph_open 客户端近似（显式 open 点、图名存在性判 cold/warm）；lru hit_ratio 改离线
  代理（闲置后首请求高耗时占比）——显式 open miss 占比答非所问被否决，JMX sidecar
  为未验证的可能性上新组件被否决。
- n_now_lag_seconds 改名 ex_last_write_age_seconds（原语义被 M10 写穿形态消灭；
  DMS 顺带发聚合 gauge，space_id 黑名单守基数）。
- ③ 口 M12 内收口（过滤器读管线 + event_id 去重 + API 发布代码删除 + 测试改定案形态）。

**踩坑（后续模块要沿用）**
- ES 排序不能用 `_id`（fielddata 限制）——日志游标按 timestamp 单字段，同毫秒边界重读
  由 event_id 去重兜底。
- colima 拉新镜像：VM dockerd 代理指向宿主机 192.168.5.2:7897，宿主机代理只监听
  127.0.0.1 时 TLS 超时——开 Allow LAN 或宿主机 crane 拉取 + docker load（prometheus/
  grafana 镜像实测）。
- Prometheus scrape 宿主机 uv run 进程：host.docker.internal 在 colima 实测可达
  （M12 集成测试实证）；prometheus_client start_http_server 默认绑 0.0.0.0 无需改。
- CI 的 `uv sync` 只装根 dev 依赖组——新包必须加进根 pyproject 的 dev 组 +
  [tool.uv.sources]，否则 CI ModuleNotFoundError（本地 uv sync --all-packages 不会暴露）。

**遗留 / 已登记缺口**
- lru 代理/touched 窗/exporter 轮询节奏等阈值全部 env 占位待标定（§20）。
- exporter counter 游标不持久化（重启全量重折叠；es-ops 滚动清理后重建成本再议）。
- es-ops 动态 mapping 开发期够用；index 模板/lifecycle 留种子期。
- API /metrics 无鉴权是 1.0 内网口径，2.0 服务商场景再议。

**下一步：M13 多租户工程红线落地**（开发文档 §14）。

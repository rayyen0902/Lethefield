# 工作日志

每个模块在独立会话中开发，本日志供新会话快速续接。长期有效的约定与环境事实以 `AGENTS.md` 为准，此处只记过程、决策与遗留。

## M0 — 工程地基（2026-08-04，commit f7a7bda，CI 绿）

**交付**
- 仓库骨架：uv workspace monorepo（Python 3.12 托管）+ ruff/pytest 基线 + Makefile；git init，仓库公开于 GitHub（rayyen0902/Leth0field）。
- 共享库三样（M0 纪律：共享代码只许这三样，各有独立单测）：
  - `libs/logschema`：结构化日志事件 schema（Pydantic，JSONL 序列化；`space_id` 在明细事件中合法，仅禁作聚合指标标签）。
  - `libs/metrics`：指标 registry 封装，命名 `lethefield_<域>_<名称>_<单位>` 与标签白/黑名单在注册时强制（`space_id`/`node_key` 直接抛错）。
  - `libs/clients`：五个客户端工厂（env 注入）+ **`ControlPlaneStore` 冻结接口**（契约 4）+ 单节点 `StaticControlPlaneStore`。
- `ops/decision_log`（§11.3）、`ops/auth_registry`（§12.4，粒度 scope 判定）最小实现：PostgreSQL 存储 + CLI。
- `docker-compose.yml` 单节点全栈 8 组件：JanusGraph 1.0.0 + Cassandra×2（cell/ex）+ ES×2（graph/ops）+ Pulsar + Redis + PostgreSQL；JG 启用 ConfigurationManagementGraph 支持动态建图。
- `tests/integration`：spike q1–q4 按文档重建（spike/ 不在仓库）——图读写 2 跳、向量索引、动态建图冷开、**四断言**（高分召回/低分过滤/衰减过滤/跨 space 零泄漏），衰减断言复刻 spike 场景（s=0.9、Δn=20 → s_eff≈0.098 被 θ 过滤）。
- `scripts/ci.sh` + GitHub Actions 工作流。
- 验证：本地 24 单测 + 10 集成全绿；GitHub Actions 一次通过（干净 ubuntu 从零起栈）。

**关键决策**
- Python 依赖管理用 uv workspace；lint 只要 ruff（lint+format），不引入 mypy（M0 保持轻）。
- `ControlPlaneStore` 接口 M0 冻结、实现随 M9——但附 `StaticControlPlaneStore`（全部映射到单一本地 cell），否则 M1+ 各模块在单节点形态下无法落地"存储访问必经抽象"这条硬约束。
- 决策留痕/授权注册表存储用 compose 里的 PostgreSQL（不用 SQLite，避免后续迁移）。
- 测试用 FF 参数占位：λ=0.16、θ_base=0.3、ρ=1（选值依据是复刻 spike 的 s_eff≈0.10 场景，正式标定属种子期）。

**踩坑（后续模块要沿用）**
- kNN 跨 space 零泄漏 = routing（分片收拢）+ `space_id` term filter（语义隔离）**双重机制**——只带 routing 会命中同分片的其他 space 文档（分片是多 space 共享的）。M4 Stage 2 必须沿用。
- Gremlin 绑定名避开保留字（`keys` 报 499）；`getGraphNames()` 等服务端返回值按元素逐个流回，不是嵌套列表。
- JanusGraph 1.0 顶点不支持 `v.both(Direction, label)` 直调，邻居扩展用 traversal `both()` 步骤。
- JG 官方镜像读取的 server yaml 固定路径是 `/etc/opt/janusgraph/janusgraph-server.yaml`（不是 conf/ 下的）；自定义 yaml 只绑 ConfigurationManagementGraph 后，gremlin 客户端别名必须指向它。
- Cassandra 官方镜像重启后 `broadcast_rpc_address` 失效（启动报 wildcard rpc_address 错），compose 必须显式设 `CASSANDRA_BROADCAST_RPC_ADDRESS`。
- spike 遗留容器（spike-elasticsearch 等）占用 8182/9042/9200 端口 + colima 曾因 8 容器并发启动卡死 daemon——重启 colima 解决；spike 容器已停未删。

**遗留**
- spike 原始脚本（q1–q4、SPIKE_REPORT.md）不在仓库，四断言是按开发文档/设计文档 §16 描述重建的；拿到原脚本后可对比参数口径。

## M1 — 存储基础设施（2026-08-04，commit b47ac08，CI 绿）

**交付**
- compose 集群命名：Cassandra `lethefield-cell`/`lethefield-ex`、ES `lethefield-graph`/`lethefield-ops`（改名需清卷重建——Cassandra 校验已存集群名）。
- `scripts/verify_isolation.py`：4 类存储物理隔离证明（Cassandra cluster_name+host_id、ES cluster_name+cluster_uuid、Pulsar admin 集群列表）。
- `scripts/check_graph_config.py`：`ids.authority.wait-time` 默认值巡检（红线 4）——静态（配置文件无显式配置）+ 运行时（逐动态图读 management 生效值）双层。
- `ops/clock_monitor`：时钟偏移监控告警（红线 6）——8 组件时钟采集（PG now()、Redis TIME、Cassandra toUnixTimestamp、Pulsar Date 头、ES 走 `docker compose exec date`、JG gremlin `new Date()`），±5s 阈值，告警输出结构化日志事件、退出码 1。
- `scripts/measure_restart_baseline.sh` + `deploy/baselines/restart_baseline.jsonl` 实测：JG 58s（spike 参考 ~52s）、ES ~40s、Cassandra ~28s、Pulsar 17s、Redis/PG ≤1s。
- `tests/integration/test_m1_infra.py` 6 项：隔离证明、红线 4 双层、健康偏移、+60min 模拟跳变告警、采集失败不静默、注入伪造源端到端。
- 验证：本地 24 单测 + 16 集成全绿；GitHub Actions CI 绿（run 30938772657）；巡检三脚本各跑一遍留证。

**关键决策**
- 时钟监控判定（`check_offsets` 纯函数）与采集（`collect_all`）分离——模拟跳变靠注入伪造样本/伪造采集器，不需要真的拨时钟；"采集失败"记为超大偏移样本触发告警，不静默跳过（与 Dead Man's Switch 同源）。
- `ids.authority.wait-time` 默认值以实测为准：JG 1.0.0 是 **300ms**（management toString = `PT0.3S`），不是凭印象的 100ms。巡检比较的是"等于默认值"，不是"等于某个文档数字"。
- ES 的 HTTP 响应没有 `Date` 头——其时钟采集改走容器系统时钟（`docker compose exec date`），语义上红线 6 监控的本来就是节点时钟。

**踩坑**
- JanusGraph Duration 的 toString 是 ISO-8601（`PT0.3S`），解析要兼容 `PT0.3S` 和 `100 ms` 两种形态（check_graph_config.py `duration_to_ms`）。
- dataclass 没有 `_replace`（那是 namedtuple 的），用 `dataclasses.replace`。

**遗留**
- 重启基线是手动脚本不进 CI（基线记录非质量门）。
- NTP 硬化本身是部署侧清单项（真实服务器部署时的配置要求），代码侧只能交付偏移监控告警；上服务器时需在部署文档中补齐 NTP 配置项。

## M2 — RMS 图 Schema（2026-08-04，commit 960600b，CI 绿）

**交付**
- 新 workspace 包 `services/rms`（`lethefield_rms`）：RMS 侧代码归属地。
  - `ensure_schema.groovy` + `schema.py`：幂等 schema 初始化（16 属性键 / 5 边标签 / byNodeKey 唯一 + byEntityKey 非唯一复合索引），CLI `python -m lethefield_rms.schema <gname>`。
  - `writer.py`：`create_event_node`（φ 初始化 n_last_touched=n_created、三计数器 0）、`create_entity_node`、`create_edge`——M15 写入链地基。
  - `vectors.py`：`rms_vectors` 独立向量索引建索引/写入/kNN（routing + space_id term filter 双机制强制）。
- `libs/clients` 新增 `gremlin_client()` 工厂（env `LETHEFIELD_GREMLIN_URL`）。
- `scripts/check_rms_schema.py`：schema 巡检 + rms_vectors mapping 校验 + ref_ex 抽样。
- `tests/integration/test_m2_schema.py` 6 项集成测试；conftest 重构为复用 lethefield_rms（spike 内嵌 schema 脚本已删，单一事实源）。
- 验证：本地 lint / 30 单测 / 22 集成测试全绿；GitHub Actions CI 绿（run 30945616671）。

**关键决策**
- `agent_actor_id` 做一等属性（设计文档 §8 明确其为节点属性），A_i 其余结构化内容进 `attrs` JSON 字符串。
- 实体顶点用 `node_type="entity"` + `entity_key` 区分，不引入自定义 vertex label。
- 向量索引 dims 参数化（ embedding 模型未定），测试用 dims=4。

**踩坑（后续模块要沿用）**
- gremlin_python 把 Python int **一律按 int32 序列化**，毫秒时间戳等大数直接溢出。Long 属性绑定改字符串传输 + Groovy `as long` 强转（writer.py 已是范例，M3/M15 照此写）。
- `lethefield_rms/__init__.py` 不做 eager 汇出（避免 `python -m` 双重导入告警），调用方一律按子模块导入。

**遗留**
- `ref_ex` 校验只覆盖 RMS 侧不变量（非空/字符串/图内唯一），EX 侧 join 待 M10 落地后补（check_rms_schema.py docstring 已注明）。
- 历次集成测试图（`m2_*`/`it_q3_*`/`it_m2_cli`）按红线 5 只 close 未 DROP，本地卷用 `make reset` 统一清。

---

**下一步：M3 FF 计算引擎**（开发文档 §4）。要点：`s_effective` 读取时现算、衰减永不写回存储（`s` 只能被 δ 三值 +0.2/−0.5/−0.1 修改）、`n_star_cached` 仅限前置粗筛、参数分层（节点属性 / agent 常数 / 查询时 ρ）不能混层。FF 现算测试基准已在 `tests/integration/ff_utils.py`（λ=0.16、θ=0.3）。

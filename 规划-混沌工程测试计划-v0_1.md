# 规划：混沌工程测试计划 v0.1（2026-08-14）

阶段 1 准出硬项（开发文档 §19：必须有书面计划）。**本文档只写计划不演练**；
实际演练在物理机（Tailscale `ubunturay`）另行安排。

依据 = 已落地的自愈与告警机制（M1–M17 定案），不新写代码。每条"预期告警"
必须指到 M12 已落地的指标/日志事件名；指不到或行为未经实测的标 **【待验证】**。

## 0. 演练通用前提

- 基线：`make reset` 清卷重起 + `lethefield_scheduler bootstrap` + 金丝雀 space
  （IS 链路开通，写入 3–5 条事件确认链路绿），复用 `scripts/smoke_real_llm.py`
  的 write/wait 相位做"演练前后链路健康探针"。
- 注入手段：docker compose stop/pause/restart（组件级）、kill（进程级）、
  timedatectl set-time（时钟）、FLUSHALL（Redis 丢键）。注入与恢复全程计时，
  RTO 记录进 `deploy/baselines/`。
- 判定纪律：预期告警未出现 = 演练失败项，如实登记，不现场改代码。
- 硬件基线（2026-08-15 用户备注）：演练机 ubunturay 开了超频。混沌演练为
  软件层故障注入、GPU 零参与，硬件烧毁风险不来自演练；但超频抖动会污染
  证据（无法区分"预期告警"与"硬件抽风"，i9-14900K 有稳定性前科）——
  **演练（及时钟敏感的场景 6）执行前应将超频恢复默认**，CI/演练机的
  价值在证据可信度不在跑分。GPU 重负载（2.0/3.0 蒸馏/场模拟）启用前另行
  评估散热与功耗墙。

## 1. 信号字典（以下名字全部已落地，场景章节只引用）

**page 级日志事件**：`ex_stream_publish_failed`（EX→Pulsar 发布失败，API 不阻塞返回）、
`ingest_probe_failed`（DMS 管道活性探针）、`training_control_backlog_stalled`（DMS 训练
控制 backlog 停滞）、`ex_n_regressed`（DMS n 一致性：Redis ex:n 回退 vs EX MAX(n)）、
`ss_scoring_dlq`、`ss_n_gap`、`writer_dlq`、`writer_n_gap`、`clock_offset_alert`
（clock_monitor，退出码 1）。

**observation 级日志事件**：`space_write_stale` / `space_write_recovered`（DMS 新鲜度）、
`ss_space_subscribe_failed`（SS 订阅未就绪，状态翻转时）、`writer_compensate_pending`
（补偿遇 details 缺失跳过等 SS）、`rebuild_scoring_missing` / `rebuild_vector_missing`
（重建保真缺口登记）。

**指标**：`lethefield_ex_stream_publish_total{result}`、`lethefield_ss_dlq_total`、
`lethefield_ss_n_gap_total`、`lethefield_ss_llm_calls_total{result}`、
`lethefield_writer_dlq_total`、`lethefield_writer_n_gap_total`、
`lethefield_writer_node_write_total{result}`、`lethefield_record_confirm_duration_seconds`、
`lethefield_ex_last_write_age_seconds{dimension}`、Prometheus `up{job=...}`
（ss/writer/fs/exporter 五个 job 已配 scrape）。

**巡检出口码**：`lethefield_ingest_dms --once`（四路，page 即退出码 1）、
`lethefield_fs.liveness`（sweep DMS，超窗退出码 1）、`lethefield_clock_monitor`、
`scripts/check_graph_config.py`（红线 4，值被改退出码 1）、
`scripts/check_es_snapshot.py`（修订记录 25 配套）。

**已知告警空白（引用工作日志缺口登记）**：API 5xx 无计数指标、retrieve 静默空
无活性探针——涉及场景一律标【待验证】，不视为已有告警能力。

## 2. 故障场景清单

### 场景 1：Pulsar 不可用

- **注入**：`docker compose stop pulsar`。
- **预期告警**：金丝雀 record 仍 200（EX ack 即返回，M14 定案）+
  `ex_stream_publish_failed` page + `lethefield_ex_stream_publish_total{result="failed"}` 增长；
  DMS 下一轮 `ingest_probe_failed` page。SS/writer consumer 断连后的重连行为
  **【待验证】**（pulsar-client 内建重连，未实测 broker 级断连恢复）。
- **预期自愈**：broker 恢复后 producer/consumer 重连【待验证】；中断窗口的
  EX 事件由 n 连续性自愈补偿——`ss_n_gap` page + SS 按 n 区间从 EX 补偿打分，
  writer 侧 `writer_n_gap` page + 从 scoring_result details 全保真补偿建点
  （details 缺失则 `writer_compensate_pending` observation 跳过等 SS）。
- **恢复校验**：`docker compose start pulsar` → 金丝雀 record 一条 →
  `smoke_real_llm.py wait` 探针确认 scoring_result + 向量到位；核对 ss_n_gap /
  writer_n_gap 事件已发且补偿完成（EX scoring_result 数 == 事件数）；
  `ingest_dms --once` 退出码 0。
- **注意**：standalone Pulsar 启动较慢，恢复校验需等 broker 就绪再判自愈失败。

### 场景 2：Cassandra 挂（分两个子场景，compose 单节点即集群级）

**2a. cassandra-ex 挂（EX = SoT）**
- 注入：`docker compose stop cassandra-ex`。
- 预期告警：金丝雀 record 直接失败（API 5xx internal）——**API 5xx 无计数指标
  【待验证】**；超窗后 DMS `space_write_stale` observation（弱信号，区分不了
  合法闲置）。EX 挂的**直接告警面空白，如实登记**。
- 预期自愈：无（EX 是同步写路径，SoT 不可降级）；恢复后写入自然复原，
  Pulsar/SS/writer 侧无积压（故障期根本无事件产出）。
- 恢复校验：start 后金丝雀 record 200 + n 连续（`ex_n_regressed` 不触发）+ 全链落图。

**2b. cassandra-cell 挂（图存储 + 控制面映射表）**
- 注入：`docker compose stop cassandra-cell`。
- 预期告警：retrieve 5xx（图遍历失败）【待验证：无指标】；writer 建点失败 →
  nack 重投耗尽 → `writer_dlq` page + `lethefield_writer_dlq_total`；
  控制面映射查询失败 → MappingCache 服务陈旧值（M9 定案：控制面故障不扩散
  数据面，缓存 TTL 内读写继续）——缓存耗尽窗口行为【待验证】。
- 预期自愈：存储恢复后，已 DLQ 的消息按 DLQ 原文重投走补全路径（幂等三分解，
  M15 定案；无独立重放工具——手工重投，已登记缺口）。
- 恢复校验：start → 重投 DLQ → 金丝雀 retrieve 命中；调度器只读命令
  （`space status`）恢复。

### 场景 3：ES 挂（es-graph）

- **注入**：`docker compose stop es-graph`。
- **预期告警**：retrieve Stage 2 失败 → 5xx【待验证：无指标、无活性探针——
  引用"检索面静默死亡"缺口登记】；writer 向量写入失败 → `writer_dlq` page。
  DMS 四路均不覆盖 ES，如实登记空白。
- **预期自愈**：进程恢复即复原（数据在卷内）。**数据丢失子场景**（删卷模拟）：
  走 `运维-runbook-ES快照备份恢复-v0_1.md` 恢复 rms_vectors——阶段 B 已实证
  无快照 = 检索面静默全灭；恢复后校验文档数 + 金丝雀 retrieve 命中。
- **恢复校验**：start → 金丝雀 retrieve（关键词路 + 向量路各一）→
  `check_es_snapshot.py` 确认快照链未断。

### 场景 4：Redis 重启丢 n

- **注入**：`docker compose exec redis redis-cli FLUSHALL`（AOF 已配，单纯重启不丢，
  丢键需显式清；这正是 M13 红线 3 配套的防御目标）。
- **预期告警**：`ex:n:{space}` 键缺失**不告警**（n_now 从 EX MAX(n) 惰性重建是
  设计路径，DMS n_consistency 明文凭据）；若键回退旧值（AOF 加载陈旧）→
  DMS 下一轮 `ex_n_regressed` page。`ex:last_write` 键丢失后 freshness 判定
  行为【待验证】。
- **预期自愈**：下次 record 时 n 从 EX MAX(n)+1 重建（无序号重复分配风险——
  这正是该巡检的回退面）；`fs:sweep:last_ok` 心跳键随下轮 sweep 重建。
- **恢复校验**：金丝雀 record 确认 n 连续无重复；`ingest_dms --once` 第 4 路
  退出码 0；retrieve 不受影响（n 域连续即可）。

### 场景 5：writer / SS 进程崩溃

- **注入**：`kill <pid>`（两个 worker 各做一次；也可叠加"崩溃期间持续写入"）。
- **预期告警**：Prometheus `up{job="ss"}` / `up{job="writer"}` = 0——scrape 已配，
  但**无 Alertmanager 告警规则【待验证】**（1.0 告警出口是 DMS/巡检退出码，
  进程活性当前无 page 出口，如实登记）；积压期间 DMS backlog 监控只覆盖训练
  控制 topic，ex-events/scoring-results 积压无告警【待验证】。
- **预期自愈**（本计划的核心自愈场景）：进程重启 → NTracker 冷启动从
  EX 最新 scoring_result / 图内 max n_created 播种 → 缺口 `ss_n_gap` /
  `writer_n_gap` page + 按 n 区间从 EX 补偿（全保真）；Pulsar 订阅 Earliest
  + 应用层幂等（SS EX 回写幂等、writer 幂等三分解）保证重投安全。
  Exclusive 订阅重连退避已落地（M11 踩坑定案）。
- **恢复校验**：重启 worker → 观察 gap page 事件 → EX scoring_result 数 ==
  事件数、图顶点数 == scoring_result 数、DLQ 计数不增。

### 场景 6：时钟偏移

- **注入**：物理机 `sudo timedatectl set-ntp false && sudo timedatectl set-time <偏移 ±10s>`。
- **预期告警**：`clock_offset_alert`（`lethefield_clock_monitor` 退出码 1，红线 6；
  ±5s 阈值）；`check_redlines.py --runtime` 同步报红。
- **预期自愈**：无自动校时——恢复 = 人工 `sudo timedatectl set-ntp true` 强制同步
  （colima VM 漂移有实测先例，M15 踩坑已登记；物理机 systemd-timesyncd 在线）。
- **恢复校验**：clock_monitor 复跑退出码 0；偏移期间摄入的事件 τ_i 审计口径
  入演练记录（不重修数据）。

### 场景 7：PG 不可用

- **注入**：`docker compose stop postgres`。
- **预期告警**：API 吊销检查 fail-closed——带 jti 的凭证全部拒绝（异常传播
  fail-closed 不静默放行，M16 定案；返回 401 还是 500 的具体形态【待验证】）；
  ops_cli 留痕预检 fail-closed 拒绝执行（退出码非 0 + 提示）；IS CLI 全部不可用。
  PG 故障本身无 page 信号【待验证】——业务表现 = 全量请求被拒，由调用方感知。
- **预期自愈**：无状态依赖，PG 起回即全部恢复（吊销列表/留痕/IS 元数据均在卷内）。
- **恢复校验**：start → 金丝雀 retrieve 带 jti 凭证恢复 200；
  `ops_cli space status` 留痕链路恢复。

## 3. 演练节奏建议

- **准出前**：7 场景全量一轮（物理机，每场景独立 make reset 基线），RTO 与
  告警命中情况入 `deploy/baselines/chaos_drill_r1.md`。
- **种子期**：每月轮换 1–2 场景（优先场景 1/5——n 连续性自愈是最高频依赖路径）；
  每次 RMS 相关变更合并后重跑场景 3 子场景（ES 快照恢复）+ 场景 5。
- **标定后**：金丝雀查询改走标定参数，`car_new` 基准用例（smoke 报告观察项 0）
  并入演练前后探针。

## 4. 本计划暴露的告警/自愈空白汇总（如实登记，不在本计划修复）

1. API 5xx 无计数指标、retrieve 无金丝雀活性探针（场景 2/3 直接告警面空白）——
   工作日志已登记，候选归 2.0 或标定期。
2. worker 进程活性无 page 出口（只有 Prometheus up，无 Alertmanager 规则）。
3. ex-events / scoring-results 积压无 backlog 告警（DMS 只覆盖训练控制 topic）。
4. DLQ 无独立重放工具（手工按原文重投，M15 已登记）。
5. PG 故障无 page 信号（fail-closed 行为正确，但"全量 401"本身无告警）。
6. 归档重放保真未实测（需 n ≥ n_star+grace_n=60 数据集，阶段 B 已登记）。

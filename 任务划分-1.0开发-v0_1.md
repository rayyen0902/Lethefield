# Lethefield — 1.0 开发任务划分（3 人）

**版本**: v0.1
**日期**: 2026-08-04
**依据**: 《Lethefield-开发文档》v1.1（模块定义、验收标准的唯一来源，本文档不重复其内容，只做归属、协作接口与顺序划分）
**人员结构**: A = RMS 负责人；B = 接口协议 / SDK / MCP 负责人；C = 平台与数据管线负责人（其余全部）
**通用纪律**: 每人的"完成"以开发文档对应模块的【验收标准】全部通过为准，不是"代码写完"；设计未覆盖的分支先升级确认，不自行拍板（开发文档文尾变更纪律对三人生效）。

---

## 1. 模块归属总表

| 模块 | 内容 | 主责 | 协作 |
|---|---|---|---|
| M0 | 工程地基 | **C** | A（spike q1–q4 转 CI 回归）；B（共享库接口评审） |
| M1 | 存储基础设施 | **C** | — |
| M2 | RMS 图 Schema | **A** | — |
| M3 | FF 计算引擎 | **A** | — |
| M4 | 检索流程（四阶段） | **A** | B（retrieve 外部入口联调） |
| M5 | MCP / SDK 接口层 | **B** | C（EX 摄入端点对接）；A（retrieve 语义） |
| M6 | FS sweep worker | **C** | A（FF 现算复用、n_star_cached 语义对齐） |
| M7 | 纠错机制（supersedes） | **A** | B（flag_conflict 入口）；C（EX 侧纠错事件入链） |
| M8 | 空间模型与鉴权（space_id） | **A** | C（EX/Pulsar 侧分区键验收项） |
| M9 | Cell 架构 + 租户调度器 | **C** | — |
| M10 | EX 存储 + Pulsar + 三存储生命周期 | **C** | — |
| M11 | 训练数据管线 | **C** | — |
| M12 | 可观测性埋点 | **C**（日志管线、registry、聚合基建） | **各自服务的埋点由各负责人落地**（A：FF/检索/图指标；B：接口延迟/限流指标） |
| M13 | 多租户红线落地 | **C**（静态扫描/巡检工具） | 全员遵守，验收时三方交叉核验 |
| M14 | SS 显著性打分服务 | **C** | A（打分稳定性验证评审——SS 输出是 RMS 的输入，A 有一票质询权） |
| M15 | 写入链 worker（事件→图节点） | **A** | C（Pulsar topic、EX 事件元数据字段对接） |
| M16 | IS 简版（账号/凭证） | **B** | C（调 M10 开通流水线） |
| M17 | 运维操作面（CLI + 留痕联动） | **C** | B（销毁/撤回处置入口与 IS 侧联动） |

**归属原则**（发生争议时按此裁决）：
- 图数据结构、FF、检索、图写入路径 → A。
- 对外契约、凭证、客户端 → B。
- 存储/消息/调度/管线/观测基建 → C。
- 跨模块的接口契约由**消费方主导验收、生产方主导实现**。

---

## 2. 四个跨人协作接口（必须在写代码前冻结，冻结后改动需三方确认）

| # | 契约 | 生产方 | 消费方 | 冻结时点 |
|---|---|---|---|---|
| 1 | **EX 事件 schema + 可信元数据字段**（含 `agent_actor_id` 盖章字段、事件类型：经验/元事件、n 序号分配语义） | C（EX 摄入） | A（M15）、B（M5 record 转发） | M0 结束 |
| 2 | **SS 打分结果 schema**（六维原始值 + 合成 s + 模型版本 + 事件引用） | C（M14） | A（M15） | M14 开工前 |
| 3 | **JWT claim 结构与校验规则**（`account_id/space_id[]/agent_actor_id/scope`，debug scope 签发管控） | B（M16） | 全员（每个受保护接口） | M5 开工前 |
| 4 | **ControlPlaneStore 接口**（space→Cell 映射抽象，M0 冻结接口、M9 落地实现） | C | A/B（所有存储访问只依赖此接口，禁止绕行） | M0 结束 |

另有一个隐含契约：MCP 四接口的错误码/限流语义由 B 定义并维护契约测试，A/C 的内部失败模式要映射到这套错误码上，不允许各服务自定义对外错误格式。

---

## 3. 顺序与阶段（依赖驱动，不是平均切分时间）

**Phase 0 — 地基（全员，C 主导）**
- C：monorepo、共享库三样、docker-compose 全栈、决策留痕表单 + 授权注册表。
- A：spike q1–q4 移植为 CI 集成基线。
- B：参与共享库与四个协作接口的评审。
- 出口：M0 验收全绿；契约 1、4 冻结。

**Phase 1 — 各自打底（并行）**
- C：M1 基础设施 → M10 的 EX 摄入路径（事件写入 + n 序号 + namespace）。
- A：M2 图 schema（依赖 M1 环境就绪）。
- B：M16 IS（账号/凭证/JWT）+ SDK 骨架；契约 3 冻结。
- 出口：EX 可写入可查询；图 schema 建库成功；能签出 JWT。

**Phase 2 — 写入链贯通（关键路径，优先资源）**
- C：M14 SS 打分服务（**打分稳定性验证在本阶段完成**，结论进决策留痕——这是全项目最大效果风险项，不允许压后）。
- A：M3 FF 引擎 → M15 写入链 worker（依赖契约 2）。
- B：M5 的 `memory.record` / `memory.flag_conflict`（转发 EX，依赖 Phase 1 的 EX 摄入）。
- 出口：一条事件从 `record` 调用到图顶点可查，全链路可追踪。

**Phase 3 — 读链路与记忆动力学**
- A：M4 四阶段检索 → M7 纠错（含 supersedes 重定向、EX 重放重建脚本）。
- B：`memory.retrieve` / `memory.reinforce` 联调、debug scope 验收、四接口契约测试补齐。
- C：M6 FS sweep → M9 调度器完整版（开通/迁移/注销全流程）。
- 出口：四接口全部可用；EX 重放重建 RMS 通过校验。

**Phase 4 — 收口与准出**
- C：M11 训练管线、M12/M13 收口（含红线自动化检查）→ M17 运维 CLI（CLI 依赖 M9/M10 流水线已可用，故排在本阶段）。
- 全员：M12 各自服务埋点补齐 → 对照 §19 验收总览逐项核验 → 混沌演练计划成文。
- 出口：§19 准出条件全勾。

---

## 4. 负载风险与调剂预案

C 的模块数量显著多于 A/B，这是"剩下的一个人"结构的固有风险，提前约定调剂顺序（仅在 C 成为关键路径瓶颈时启用，由项目负责人拍板）：

1. **第一转移项：M12 收口 → B**。B 在 Phase 3 后余量最大，埋点收口（聚合基建、Grafana 看板、红线巡检脚本）与接口工作耦合度低，适合整体移交。
2. **第二转移项：M14 打分稳定性验证 → A 主审**。验证报告本就要求 A 参与评审，C 过载时验证执行可由 A 接手，C 只保留服务实现。
3. **不可转移项**：M9/M10（Cell/EX/调度器，C 的上下文不可替代）、M11（训练管线，与授权注册表/注销广播耦合最深）。

反向风险：A 是 Phase 2–3 关键路径（FF→写入链→检索→纠错全部串行在 A 身上）。若 A 成为瓶颈，M6 FS sweep 本就归 C，M7 的 EX 重放重建脚本可移交 C（纯数据工程，不涉图语义）。

---

## 5. 周会机制（建议，非强制）

- 每周一次三方同步，固定议程：① 四个协作契约有无变更需求；② 各自模块对开发文档验收标准的完成度（按 checkbox 过）；③ 有无触发"升级确认"的设计分支。
- 契约变更必须当周同步、三方签字（决策留痕表单记录），不允许私下改 schema。

---

*本文档只做任务划分；模块的【目标】【验收标准】【明确不做】一律以《Lethefield-开发文档》v1.1 为准。划分发生调整时更新本文档，不动开发文档。*

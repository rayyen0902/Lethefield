# 存档：外部方案评估——"数十亿 Space + per-space 全物理隔离"架构

**评估时间**：2026-07（对应设计文档 v0.12）
**评估结论**：**不采纳为默认架构，维持 v0.8 逻辑隔离定案**（Cassandra `space_id` 分区键 + ES 共享 index custom routing）；物理隔离保留为 per-space 企业级 SLA 局部升级逃生门（§7.4 已有）。方案中有价值的两条工程红线已吸收进 §11.5。

---

## 一、方案原文（完整保留备查）

> 核心共识先敲定
>
> 数十亿级 Space 规模这个体量，逻辑隔离、全局一张大图、共享存储分区方案直接宣判不可行。
> 你的核心判断完全正确：
>
> 全局总量巨大的场景，性能瓶颈从来不是「单个Space资源开销」，而是全域数据耦合带来的长尾阻塞、扫描放大、索引膨胀、资源相互污染。
> 彻底隔离架构带来最重要的收益：
> 单个Space的检索、图遍历、ES向量召回、活跃度状态场计算，计算边界被锁死在自身内部；性能上限只由当前Space自身数据量决定，和系统总共存在几十亿个Space毫无关系。
>
> 下面结合：数十亿Space + JanusGraph DynamicGraph（ConfiguredGraphFactory）+ 一Space一独立Cassandra Keyspace架构，分析规模化瓶颈、架构取舍、工程防线、向量召回适配方案。
>
> 一、先直面最现实的硬核矛盾：数十亿 Space × 独立Keyspace
>
> Cassandra 存在一个关键限制：单集群能够稳定承载的 Keyspace 数量有上限
> 业界实测经验：
>
> - 稳定安全区间：单Cassandra集群 < 1000～2000 个Keyspace
> - 突破5000个Keyspace：节点元数据（schema、分区元信息）持续膨胀，启动变慢、节点重启阻塞、 gossip压力飙升
>
> 👉 绝对不能用单一Cassandra集群承载数十亿Space
> 解决方案：Cassandra集群分片池（租户打散多集群）
> 自动化调度层能力：
>
> 1. 预先部署N组对等Cassandra集群池；
> 2. 创建Space时，调度算法把space均匀分配到负载最低的集群；
> 3. 不同集群之间完全独立，互不感知；
> 4. 支持后期租户跨集群迁移（整套Keyspace迁移）。
> 本质：把"Keyspace数量压力水平分摊到大量独立存储集群"，规避Cassandra元数据上限。
>
> 同理 Elasticsearch：
> 每个Space独立索引，严禁单ES集群承载海量索引。必须构建ES集群池，租户索引跟随Space调度到对应ES集群。
>
> 二、两条部署路线重新评估（适配数十亿Space远景）
>
> 方案A：共享Gremlin集群 + DynamicGraph（长期不适合数十亿Space远景，只能作为过渡原型）
>
> 致命短板：
> Gremlin JVM进程内会缓存所有打开过的Graph实例连接。
> 一旦存在大量活跃Space，连接数、配置缓存、线程句柄持续上涨。
> 海量Space随机访问场景，频繁 open()/close() 带来巨大连接重建开销。
>
> 方案B：Space无常驻Janus实例（休眠冷启动架构，面向数十亿Space最优解）
>
> 思路推翻传统"长期驻留Pod"模式：
>
> 1. 99%的Space绝大部分时间处于冷态：几乎无访问、仅少量后台定时任务（活跃度衰减计算）
> 2. 只有收到API请求时，临时拉起JanusGraph进程/Pod， open(space) ，完成查询/写入，任务结束后立刻释放实例、关闭连接
> 3. 高频活跃大客户：设置策略，长期保留常驻Pod，免去冷启动开销
>
> 优势完美匹配你的业务：
>
> 1. 数十亿Space不需要同时运行数十亿Janus实例；活跃实例数量和并发QPS成正比，不取决于Space总存量
> 2. 天然实现算力隔离，冷态资源全部释放；
> 3. 单个Space崩溃、内存泄漏，进程销毁自动隔离，不会影响任何其他租户。
>
> 补充：冷启动延迟问题对策
>
> - Redis缓存热点元数据、高频记忆节点活跃度，优先响应；
> - 后台批处理任务（记忆衰减、状态场迭代）采用轻量常驻worker池，不和在线查询争抢实例。
>
> 三、架构如何解决你核心业务链路：图遍历 + ES向量召回
>
> 你的链路标准流程：
>  请求 → API网关 → ES向量/文本召回候选记忆节点ID → JanusGraph子图遍历（时序/因果/语义关联）→ 状态场活跃度过滤
>
> 在「独立Space隔离架构」下的巨大优势
>
> 1. ES层面：每个Space独立索引
> 向量搜索严格限定在当前租户索引内，不存在跨索引检索、不需要增加租户过滤条件。
> 向量查询不会扫描全局海量数据，向量召回耗时只取决于这个Space内部记忆规模。
>
> 如果是共享索引方案：查询必须携带space_id做过滤，向量相似度计算后再过滤，大量无效候选，延迟飙升。
>
> 2. JanusGraph图遍历层面
> 不需要每条顶点、边携带 space_id ，遍历范围天然被Keyspace锁死；
> 多跳因果图、时序图遍历，搜索边界天然闭合，杜绝潜在跨空间扫描漏洞。
> 3. 活跃度状态场计算
> 状态传播、记忆衰减任务严格局限单Space；
> 超级节点、高频更新产生的墓碑、压缩IO，封闭在自身Keyspace，不会扩散。
>
> 四、必须设立的4条工程红线（超大SaaS避坑）
>
> 红线1：严格禁止全局广播、全集群扫描操作
>
> 运维后台、数据分析程序，绝对不能执行跨Space批量查询。所有任务入口必须绑定指定space列表。
>
> 红线2：完善Space资源配额机制
>
> 虽然空间物理隔离，但依然要做租户限额：
>
> - 单个Space最大顶点数量、边数量上限
> - ES索引最大存储、向量条数上限
> - 单次Gremlin遍历最大跳数、最大返回节点数
> 防止单个客户疯狂写入，无限膨胀自身Keyspace，耗尽对应存储集群分片磁盘。
>
> 红线3：分层冷热策略
>
> 数十亿Space一定会出现极强冷热分化：
>
> - 热Space：频繁对话、记忆持续激活；常驻计算资源
> - 冷Space：数月无访问
> 冷Space策略：
>
> 1. Cassandra：调低副本数、调整压缩策略
> 2. ES：可将冷索引归档、冻结，降低资源占用
> 3. 定时活跃度后台任务降低执行频率
>
> 红线4：谨慎使用 ConfiguredGraphFactory.drop()
>
> 前文提到官方drop接口存在稳定性隐患。
> 标准化租户注销流水线（强合规）：
>
> 1. 调用ConfiguredGraphFactory.drop(graphName)
> 2. 调度脚本直连Cassandra，执行 DROP KEYSPACE IF EXISTS space_xxx
> 3. 删除对应全部ES索引
> 4. 清理Redis内该space全部缓存
> 五步校验，确保不存在任何数据残留，满足数据删除合规。
>
> 五、针对四层图（事实/时序/语义/因果）架构再次确认最优范式
>
> ✅ 维持方案：1 Space = 1 DynamicGraph（独立Keyspace），graph_type区分四张逻辑子图
> ❌ 不要拆分4个独立Graph
> 如果一个Space拆4套Graph：
> Space数量数十亿，整体资源、配置、索引数量直接×4，复杂度完全失控。
>
> 上层API对外依然暴露四张独立图库接口，网关层自动拼接 graph_type 过滤条件，业务无感知。
>
> 六、总结提炼你的架构顶层价值（可以直接用于方案PPT）
>
> 1. 规模解耦
> 系统总Space数量（几十亿）不再影响单次查询性能；单次请求性能仅由当前访问Space自身数据规模决定，理论上具备无限横向扩容能力。
> 2. 链路简化
> 向量召回、图遍历无需携带租户过滤条件，减少计算开销；消除大规模集群下过滤带来的长尾延迟。
> 3. 故障域最小化
> 慢查询、超级节点、高频更新、程序异常全部封闭在单个租户边界，无"噪声邻居"。
> 4. 生命周期极简
> 租户开通=新建独立资源集合；注销=整体销毁整套资源；支持整套Space跨集群迁移，不存在数据割裂。
>
> 后续可以深入落地的方向，你任选其一：
>
> 1）多集群Cassandra+ES租户调度器高层设计（伪代码/架构图文本版）
> 2）Space冷热分层自动化策略（活跃度驱动的资源升降配）
> 3）「ES向量召回 + Janus子图遍历」标准查询链路封装方案
> 4）JanusGraph冷启动架构完整工作流设计

---

## 二、评估结论（v0.12 评审记录）

**不采纳为默认架构。** 逐条理由：

1. **规模前提与文档测算矛盾**：方案以"数十亿 Space"为设计输入，但 §7.2 测算为 1000 用户起步、一空间≈一用户/一项目。数十亿 space 意味着数十亿用户，不是 1.0/2.0 的假设。为此预建多集群调度层属于过度工程，与 §11.4"1.0 保持简单、种子期当磨合期"的精神冲突。
2. **关键技术论据有误**：方案称共享索引"查询必须携带 space_id 过滤、相似度计算后再过滤、大量无效候选"——这是对裸逻辑过滤的批评，对 v0.8 定案的 **custom routing 不成立**。routing 使同一 space 文档（含向量）物理落在同一分片，查询带 routing 直接命中该分片，无跨分片扫描、无后置过滤，kNN 仅在单分片 HNSW 图内执行，检索精度与开销等同独立索引（§7.4 已完整论证）。
3. **自身算术否决自身**：Cassandra 的真实开销单位是 table 而非 keyspace（JanusGraph 每图约 6 张表，各有 memtable 开销）。按其自引上限（单集群 <2000 keyspace），数十亿 space 需数百万个 Cassandra 集群与数百万个 ES 集群，调度器本身即是不存在的超大型分布式系统。反观逻辑隔离方案：无论多少 space 永远一个 keyspace、一套 index，space 数量增长只转化为数据量增长，靠加节点解决——在"元数据爆炸"维度上反而比物理隔离更能承载海量 space。
4. **冷启动延迟被严重低估**：JanusGraph 开图 = JVM 启动 + Cassandra 连接池建立 + schema 校验，秒级到十几秒。per-request 冷启动对实时交互场景（陪伴型 Kin）致命，Redis 缓存无法缓解图遍历本身的开销。
5. **内部矛盾**：一面以"遍历无需租户过滤"为核心收益，一面又提出四张逻辑子图用 `graph_type` 过滤、网关拼接——`graph_type` 过滤与 `space_id` 过滤成本形状相同。且其"四层图（事实/时序/语义/因果）"与本文档定案（时序/语义/因果/实体）不符，系通用模板而非针对性分析。
6. **真实价值已覆盖**：物理隔离的故障域最小化、"DROP KEYSPACE = 合规删除"的生命周期简洁性属实，但 §7.4 已保留该路径为 per-space SLA 局部升级逃生门——方案把例外当成了默认。

**已吸收进文档的部分**：红线 1（禁止跨 space 全局扫描）、红线 2（单 space 资源配额）、红线 3（冷热分层，按本文档技术栈约束改写）→ §11.5。

**重新评审触发条件**：当"per-space 物理隔离"成为真实付费需求（企业级客户专属资源/合规硬隔离），或共享集群出现无法通过扩容解决的噪声邻居问题时，回到本文档重新评估。

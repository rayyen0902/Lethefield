# 留言板 — 致后端主体负责 Agent v0_1

**版本**: v0.1
**性质**: 任务交接留言板（非设计文档、非规划；按条执行，执行完勾除并留痕）
**日期**: 2026-08-23
**委托人**: 项目所有者（经参谋会话整理）
**状态约定**: 每条完成后在条目末尾标注 `✅（commit <sha>）`；有异议不执行，改在条目下以 `> 异议：` 回复并说明理由，等所有者裁决。

---

## 一、文档债务清偿（最优先，纯文档同步，不动代码）

**背景**：1.0 已准出闭合（M0–M17 全落地，§19 十条准出条件全部满足，所有者 2026-08-15 签署，基线标签 `stage-1-exit`，留痕 decision_log id=15），但对外文档仍停留在 M12。事实源以《工作日志.md》M13–M17 各节及「阶段 1 收官 — 准出官宣」节为准。

### 任务 D1：README.md 进度同步

- 「当前进度：M0–M12 已完成（CI 全绿），下一模块 M13」更新为：**M0–M17 全模块落地，阶段 1（技术落地验证）已准出闭合（2026-08-15，基线标签 stage-1-exit），当前处于阶段 2（种子期）规划期**。
- 「仓库结构」节 `services/` 行补全 M13–M17 新增：`ss`（M14 显著性打分）、`is`（M16 身份与凭证）；`training`、`scheduler` 等已有行核对是否与最终交付一致。
- 巡检/命令清单核对 M13–M17 新增脚本（check_space_filter.py、check_redlines.py、verify_metrics_queryable.py、check_es_snapshot.py、smoke_real_llm.py、rebuild_fidelity_drill.py 等），缺者补录或加注「完整清单见 AGENTS.md」。
- 「M10 迁移演练」段落核对：准出档（ex2 profile 双 EX 集群 + sstableloader 流式传输 + exit 演练 test_m10_migration_drill_exit.py）是否需在 README 提及，按现有详略风格裁决。

### 任务 D2：开发文档阶段状态同步

- 文首/阶段边界节：标注阶段 0/1 全部模块（M0–M17）已交付并准出闭合；指向《准出评审报告-v1_0.md》（官宣栏）与工作日志收官节。
- 核对 M13–M17 各模块节的「遗留 / 已登记缺口」是否已汇总进阶段 2 规划输入（已知边界清单：§20 待标定参数、API 多 Cell 连接路由、告警空白 6 条、观察项 7 条）。

### 任务 D3：准出状态可发现性

- 《准出评审报告-v1_0.md》头部官宣栏已更新则无需动；README 顶部加一行状态徽章式说明（如「阶段 1 已准出 · stage-1-exit」）便于外部读者 30 秒内理解项目状态。

## 二、与前端设计文档的既有交接（提醒，不新增）

《前端设计-客户端与宿主接入-v0_1》文首「给后端主体维护方的交接说明」B1–B5（§9 收窄为 HTTP+JSON API、MCP 降级远期可选、M5 改名对外 API 层、勾除「不能依赖 LLM 主动上传」批注、README api 行同步）仍然有效。**执行顺序建议：先做本文档 D1–D3（事实同步），再做 B1–B5（定稿修订，走修订记录）**，两批分开提交，留痕清晰。

## 三、阶段 2（种子期）规划输入——仅需确认收悉，暂不执行

以下三条线已在前端/愿景文档中登记，阶段 2 规划由所有者发起。后端主体**现在只需确认这些缺口已在册**，不提前动工（红线：阶段边界严格执行）：

1. **参数标定（最高优先）**：§20 占位参数全量待标定；阶段 A 冒烟两条观察项（λ=0.16 下有效记忆窗仅 2–3 事件；DeepSeek 对日常事件打分系统性偏低 12/20 被 θ_base 过滤）是标定第一输入；car_new 已标记为种子期标定第一优先级基准（smoke_real_llm_v1.md）。
2. **检索面活性探针**：阶段 B 缺口登记——向量侧故障时 retrieve 静默返回空，无法区分「无相关记忆」与「检索面死亡」；金丝雀查询探针候选归 2.0 或标定期。前端 dsh 插件的故障语义设计依赖此信号的口径，请在阶段 2 评审时给结论。
3. **边缘剖面（本地部署）**：愿景文档 §4.1 已登记课题（最小认知内核资源下限）。当前不动工；仅要求：后续所有涉及存储/组件的技术留痕中**增加一句「边缘形态下的可替换性」注记**（一句话即可）。另注意：§7.1「1.0→2.0 同一套系统不涉及技术栈迁移」的不变量表述，未来若立边缘剖面课题，需正式修订为「接口契约与认知语义不变，存储驱动可按部署剖面替换」——**现在不修，立项时再修**。

## 四、执行纪律提醒（沿用既有红线，无新增）

- 文档同步不改语义：D1–D3 只做事实对齐，凡发现文档与代码行为不一致，**以代码与决策留痕为准**，并单独记录发现，不顺手改行为。
- 环境性抖动前科：全量 CI 若遇 m8/m9/q3 族 Cassandra schema 操作超时，按 AGENTS.md 惯例先 `make reset` 复跑再定性。
- 本留言板执行完毕后，文件名加「存档-」前缀归档，执行过程的新发现回写本条或新开课题文档。

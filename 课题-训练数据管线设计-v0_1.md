# Lethefield — 训练数据管线设计（§12.2 补完）

**版本**: v0.1（提案，未定稿）
**状态**: 规划阶段最后一份设计稿——评审通过后入档，规划阶段宣告收口
**关联**: Lethefield-RMS-设计文档 v1.6（§11.2、§11.3、§12.1、§12.3、§13.4、§14、§19）
**日期**: 2026-07-29

---

## 1. 问题定义

§12.2 遗留三块：标注加工流程、高价值时刻判定规则、归档格式与生命周期。边界已由既有定案框死：

- §12.1：训练数据与运维日志是**两条独立管线**，不可混用一套埋点；训练管线载体为独立 topic + 对象存储
- §12.3（v1.5）：**全线接入**——可使用含用户记忆内容的数据（EX 副本/加工品），附三条硬义务（授权真实性、撤回与删除权联动、范围限 1.0 自有集群）
- §19.6：日志事件 schema 已定为本管线的输入接口之一
- 红线 1：任何聚合/加工不得扫描业务存储

设计目标一句话：**让"高价值判断时刻"以结构化、可撤回、成本可控的方式沉淀为训练资产，管线自身永不需要专职看护。**

## 2. 数据源清单（四个入料口）

| # | 数据源 | 形态 | 价值密度 | 授权口径 |
|---|---|---|---|---|
| 1 | **决策留痕对比数据**（§11.3："Agent 建议 vs 人类最终决策"+ 执行结果） | 结构化事件 | 最高——天然带"问题→诊断→决策→结果"骨架 | 运维元数据，无用户内容 |
| 2 | **故障与混沌工程案例**（§11.4 演练 + 真实事故复盘） | 复盘文档 + 指标快照 | 高——含未见模式 | 运维元数据 |
| 3 | **检索质量与 FF 标定明细**（§19.2 的 space 粒度日志事件：θ 过滤比、召回后被引用/被纠错明细） | 日志事件（§19.6 schema） | 中高——FF 调参与检索模型改进的依据 | 含行为元数据，按 §12.3 授权 |
| 4 | **用户记忆内容副本**（EX 事件的加工品：纠错链、supersedes 前后对、高质量问答片段） | EX 派生副本 | 训练记忆相关模型的直接原料 | **仅 §12.3 授权条款覆盖的 space** |

第 4 类是 v1.5 全线接入的落点，也是本管线与 EX 的唯一接触面：**只读 EX、产出独立副本，永不回写**。

## 3. 高价值时刻判定规则

判定分两档：**规则触发**（开发期实现，确定性）与**模型辅助**（种子期后引入，概率性）。

规则触发清单（初值，每条挂数据源）：

| 规则 | 数据源 | 理由 |
|---|---|---|
| R1 人类否决/修改 Agent 建议（outcome=rejected/modified） | 1 | 分歧即学习信号，§11.3 已定为 Agent 校准核心 |
| R2 §11.2"必须升级"四类事件（触 EX 写路径、跨 space、未见错误模式、Agent 低置信） | 1/2 | 升级事件天然是难题样本 |
| R3 召回内容被纠错（`retrieve_corrected_total` 对应的明细事件） | 3/4 | 检索失败的一手现场 |
| R4 supersedes 链形成（纠错/纠错的纠错） | 4 | 记忆演化的真值标注，人工标不出来的东西 |
| R5 混沌演练与真实事故（每次复盘必产出） | 2 | §11.4 的既定产出物 |
| R6 人类未否决但事后证明错误（回溯发现，种子期后启用） | 1 | 防"人类永远对"的标注偏差 |

**刻意不做"全量沉淀"**：信号密度优先于数据量（§12.1）——六条规则未命中的常规流量不进训练管线，只进运维日志（数周至数月滚动清理）。

## 4. 管线架构

```
数据源 1/2（留痕服务）──┐
数据源 3（日志事件流）───┼─▶ 训练 topic（Pulsar 独立 namespace）─▶ 加工 worker ─▶ 对象存储（热/温/冷分层）
数据源 4（EX 只读副本）──┘        （与业务流 retention/配额隔离，§7.5.2）     （标注、脱敏校验、格式封装）
```

- **传输**：Pulsar 独立 namespace（训练专用 tenant），与业务事件流 retention/配额隔离（§12.2 第 1 条 v0.7 已定案）——管线故障/积压不影响业务
- **加工 worker**（轻量定时 + 事件驱动混合）：格式封装（§5 schema）、授权校验（§6）、批次落盘。**离线工作全部从 topic/对象存储取数，不查业务库**（红线 1）
- **存储**：对象存储为主体（热层保留近 90 天供标注与回放，温层压缩归档，冷层深度归档）——成本结构与 §15.2 四层模型同哲学

## 5. 标注加工流程

**样本 schema**（统一封装，数据源 1/2 天然符合，3/4 由加工 worker 补齐字段）：

```json
{
  "sample_id": "uuid", "source": "decision_log|incident|ff_metric|ex_derived",
  "rule": "R1", "space_ref": "opaque_hash",
  "problem": {}, "diagnosis": {}, "decision": {}, "outcome": {},
  "auth_scope": "granted|ops_only", "created_at": "", "review": {"status": "pending|approved|rejected", "by": ""}
}
```

- **`space_ref` 用不透明哈希**：样本不暴露 space_id 明文，但哈希可支撑 §6 的撤回定位（同一 space 的样本可被成组找出）
- **标注流程**：真人决策岗在处置动作时**顺带标注**（决策留痕表单即标注界面，不新增工种）；模型辅助预标注（种子期后）+ 人工抽检（初值 10%）；`review.status` 未经 approved 的样本不进正式训练集
- **质检**：每周抽样复盘驳回率；驳回率高的规则触发判定规则修订（规则本身是活文档）

## 6. 授权撤回与删除权联动（§12.3 硬义务 #2 的落地）

**两条触发路径，同一套处置**：

1. **用户撤回授权** → 该 space 即刻停止新增第 3/4 类采样（加工 worker 查授权注册表）→ 存量样本：`space_ref` 哈希成组定位 → 内容字段清除、保留脱敏元数据骨架（问题/决策/结果不含用户内容的部分可留，因为它们的价值在"判断过程"而非"记忆内容"）
2. **整 space 销毁**（§13.4）→ 注销流水线新增一步：**向训练管线广播销毁指令** → 同上等效处置 + 授权注册表项删除

**实现要点**：

- **授权注册表**（调度器元数据的一部分，§17.2）：`space_id → {auth_scope, granted_at, revoked_at}`，加工 worker 每批次必查——未授权 space 的第 3/4 类数据**在入 topic 前就被拒**，不是进来再删
- **可定位性是可撤回性的前提**：`space_ref` 哈希 + 样本索引清单（对象存储清单文件）使"找出某 space 全部样本"是 O(清单) 操作而非全量扫描——这是本管线为 §12.3 付出的核心设计成本，必须在 1.0 内建，后补等于重建
- 处置动作本身进决策留痕（谁触发、影响多少样本、何时完成）——删除权在训练侧同样可审计

## 7. 归档格式与生命周期

- **格式**：JSONL（热/温层，人读友好、工具链通用）+ Parquet（冷层，列存压缩，供批量训练取数）。**开放格式是 §14 哲学的延伸**——训练资产同样不得被专有格式锁定
- **分层与 TTL**：

| 层 | 内容 | 介质 | 保留 |
|---|---|---|---|
| 热 | 近 90 天样本（待标注 + 已标注） | 对象存储标准层 | 90 天滚动 |
| 温 | 已 approved 样本主集 | 对象存储低频层 | 长期 |
| 冷 | 历史快照（年度打包） | 深度归档 | 长期，年度成本复核 |
| 废弃 | rejected 样本、被撤回清除内容的骨架 | 低频层 | 1 年（审计需要）后清除 |

- **成本控制纪律**（§12 状态说明的落地）：温/冷层只进 approved 样本，是"信号密度优先"在存储层的执行；每年复核一次冷层取数频率，连续两年零取数的快照降级或清除（决策留痕）

## 8. 一致性检查

- §12.1：两管线物理分离——本管线有自己的 topic/worker/存储，与运维日志（ES 日志集群）零共享组件
- §12.3：三条硬义务全部落到机制（授权注册表、撤回处置路径、1.0 范围限定）
- §13.4：销毁流水线增加"训练管线广播"步骤——§17.2/§18.3 的注销流程需同步补此步
- §14：开放格式 + 可撤回可审计，与可携带性同哲学
- §19：复用日志事件 schema 为数据源 3 的入料接口；本管线自身只要求 2 个指标（`training_pipeline_backlog`、`training_samples_total{source,rule,review_status}`）纳入 §19.3
- 红线 1：全管线无业务存储扫描路径

## 9. 1.0 最小实现 vs 延后

**开发期必做**：训练 topic 与加工 worker 骨架、样本 schema 与授权注册表、R1/R2/R3 三条规则、热层落盘、撤回/销毁联动（§6 全部——这是义务不是功能）。
**可延后**：R4–R6、模型辅助预标注、温/冷分层自动化（种子期前补）、Parquet 冷层（有训练需求时补）。

## 10. 待决项

1. 授权注册表与调度器元数据的归属细节（并入 §17.2 映射表还是独立表——倾向并入，定稿时拍板）
2. 模型辅助预标注的启用时机与模型选型（种子期后）
3. 冷层年度成本复核的责任人（运维模型 §11.1 分工待明确）

---

*本稿为 §12.2 补完设计，评审通过后以新版本号并入主文档；§10"训练数据归档管线"勾除后，规划阶段设计任务全部清场，进入 1.0 开发。*

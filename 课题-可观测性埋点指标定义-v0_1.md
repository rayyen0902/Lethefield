# Lethefield — 可观测性埋点指标定义

**版本**: v0.1（提案，未定稿）
**状态**: 规划阶段设计任务（§10，v1.5 立项）——现在定义、1.0 开发期落地实现
**关联**: Lethefield-RMS-设计文档 v1.5（§2 FF 参数、§11.3、§11.4、§12.1、§13、§15.3、§16、§17.3）
**日期**: 2026-07-29

---

## 1. 设计原则

1. **指标为决策服务**：每个指标必须挂在一个明确的消费方上——某个待标定参数、某条告警、或 §11.3 决策留痕。不允许"先采了再说"的虚荣指标（§12 状态说明：数据分层不先行，日志基础设施会变成需要专职看护的系统）
2. **三线分类**：每个指标标注用途线——**告警线**（实时，驱动 PM Agent/真人响应）、**标定线**（离线分析，驱动参数调整）、**留痕线**（审计与 Agent 校准数据）
3. **基数纪律**：`space_id` **不得**作为聚合指标的标签（数十亿 space 远景下标签基数爆炸直接拖垮指标系统）。space 粒度的明细走**事件/日志管线**（§12.1 的 ES 日志集群），聚合指标只保留低基数标签（service、cell_id、tier、result 类）
4. **工具中立 + 推荐实现**：指标模型按 OpenMetrics 语义定义（counter/gauge/histogram），推荐实现 Prometheus + Grafana（1.0 单节点起步）；服务商 2.0 可替换为其监控体系，指标定义不变

## 2. FF / 记忆质量指标（标定线为主）

FF 参数标定的验收指标（§10 参数标定的输入）。数据源：retrieve/record/reinforce 服务在请求路径上同步打点到日志管线（space 粒度明细），聚合指标由离线任务从日志产出（避免实时跨 space 扫描，红线 1）。

### 2.1 λ（衰减率）与 t₀ 的标定指标

| 指标 | 类型 | 定义 | 判读 |
|---|---|---|---|
| `ff_theta_filter_ratio` | histogram（按 retrieve 请求） | 单次 retrieve 子图中被 θ 硬过滤的节点比例（Q4 spike 链路实测值：15 节点 filtered 8 ≈ 0.53） | 长期 ≈0 → 衰减太慢（什么都能召回）；长期 ≈1 → 衰减太快（什么都召回不了）。健康区间待种子期标定 |
| `ff_s_effective_dist` | histogram | 检索返回节点的 s_effective 分布（离线按 space 聚合后再汇总） | 分布整体贴近 θ → 大量记忆处于"濒死"状态，λ 偏大或强化太少 |
| `ff_recalled_then_touched_rate` | gauge（离线日更） | 被召回节点在 T 窗口（初值 24h）内被 reinforce/引用的比例 | **λ 的核心验收指标**：低 = 召回的是无用旧记忆（λ 太小或语义图质量差）；此指标应与 λ 调整形成闭环 |

### 2.2 δ 三值（+0.2/−0.5/−0.1）与 N_neglect 的标定指标

| 指标 | 类型 | 定义 | 判读 |
|---|---|---|---|
| `ff_delta_applied_total{type}` | counter（type=reinforce/conflict/neglect） | δ 调整触发次数 | 三类比例失调提示信号源配置问题（如 neglect 占 99% 说明 sweep 过激进） |
| `ff_s_clamp_total{bound}` | counter（bound=upper/lower） | s 触及上下限被截断的次数 | 截断频繁 → δ 步长与 s 区间不匹配（+0.2 在 [0,1] 区间下 5 次即饱和，需评估是否改非线性 δ） |
| `ff_conflict_then_superseded_rate` | gauge（离线） | 被 −0.5 冲突减分的节点后续被 supersedes 的比例 | −0.5 的校准验证：高 = 减分命中真实错误（值合理）；低 = 误伤（考虑调小或改进冲突判定） |
| `ff_neglect_then_revived_rate` | gauge（离线） | 被 −0.1 忽视惩罚的节点在惩罚后 T 内被再次使用的比例 | N_neglect 的校准验证：高 = 惩罚窗口太短（误伤活跃记忆）；≈0 且检索质量差 = 窗口太长或惩罚太轻 |

### 2.3 θ_base 与检索质量综合指标

| 指标 | 类型 | 定义 | 判读 |
|---|---|---|---|
| `retrieve_empty_result_total` | counter | retrieve 返回空结果次数（按 tier 标签） | 零命中率持续高 → θ_base 过高或记忆写入不足 |
| `retrieve_corrected_total` | counter | 被召回内容随后被纠错（supersedes）次数 | **检索质量的终极指标**：召回即错误 = 检索链路或 supersedes 重定向（§5 Stage 3）失效 |
| `retrieve_anchor_kept_ratio` | histogram | 锚点节点中通过 θ 过滤的比例 | 低 = ES 向量召回的候选质量差（向量模型问题）或衰减过强 |
| `retrieve_kept_then_used_rate` | gauge（离线） | 通过过滤的节点在后续对话中实际被引用率 | 全链路端到端验收 |

## 3. 系统 / SRE 指标（告警线为主）

按七服务 × 黄金信号（延迟/流量/错误/饱和）铺开，下表只列**项目特有、非教科书**的部分（通用 CPU/内存/磁盘略）。

| 指标 | 类型 | 消费方 |
|---|---|---|
| `graph_open_duration_seconds{type}`（type=cold/warm） | histogram | §15.2 四层模型验证（基线：cold p50≈3.6s、warm p50≈0.48s，§16.2）；漂移告警 |
| `graph_lru_cache_hit_ratio` | gauge | LRU 缓存大小与预热策略标定（§17.2） |
| `retrieve_stage_duration_seconds{stage}`（stage=knn/subgraph/ff_filter） | histogram | 检索链路分段延迟，定位劣化段 |
| `record_confirm_duration_seconds` | histogram | §9.3 record 同步等 EX 落库确认的延迟预算 |
| `pulsar_backlog_events{namespace_class}` | gauge（按 namespace 类别聚合，不按 space） | **Dead Man's Switch 实现载体**（§7.5.1）：活跃 space 窗口内零摄入告警 |
| `ex_write_duration_seconds` | histogram | EX 摄入链路健康（source of truth 写入路径，§11.2 升级清单第 1 条关联） |
| `n_now_lag_seconds` | gauge | Redis 缓存的 n_now 与实际事件序号的滞后（§13.2），影响 FF 现算精度 |
| `fs_sweep_lag_seconds` / `fs_sweep_processed_total` | gauge / counter | FS 后台 sweep 健康（§13.3 忽视惩罚、§13.5 职责） |
| `cell_watermark{cell_id,dimension}`（dimension=keyspaces/shards/disk/heap） | gauge | §17.3 水位制调度的直接输入 |
| `space_storage_bytes{tier}` | gauge（离线汇总） | **单 space 月成本测算**（§15.3 成本边界验证，EX 合并口径 §18.1） |

## 4. 留痕线指标（§11.3 的量化）

| 指标 | 定义 | 消费方 |
|---|---|---|
| `agent_suggestion_total{outcome}`（outcome=accepted/modified/rejected） | PM Agent 建议的人类处置结果计数 | Agent 置信度阈值校准的核心数据（§11.3）；accepted 比例持续 >95% 的类别可评估放宽升级门槛 |
| `escalation_total{reason}`（reason 对应 §11.2 清单四类） | 升级事件计数 | 升级清单本身的修订依据；reason 分布偏移提示系统行为变化 |

## 5. 埋点实现规范

- **命名**：`lethefield_<域>_<名称>_<单位>`，单位用 Prom 标准（seconds/bytes/total/ratio）
- **写入路径**：请求路径同步打点只写**聚合指标**（进程内 registry，O(1) 开销）；space 粒度明细写**结构化日志事件**（异步批量进 §12.1 ES 日志集群）；离线聚合任务从日志产出 §2 的 gauge 类标定指标——**聚合永远走旁路，不扫存储**（红线 1）
- **标签白名单**：service、instance、cell_id（2.0 启用）、tier、result/type/reason 类枚举；**黑名单**：space_id、node_key、内容摘要（同时防基数爆炸与训练管线边界混淆，§12.3）
- **与训练管线的边界**：本规范的指标与日志是运维/标定用途；含用户内容的副本进训练管线须走 §12.3 授权口径，两条通路在埋点代码层面物理分开（§12.1）

## 6. 1.0 开发期落地清单（最小集）

开发期**必须随服务实现**的（缺了等于服务未完成）：

1. §3 全部系统指标（告警线——烟测与混沌工程的观察手段）
2. `ff_theta_filter_ratio`、`ff_recalled_then_touched_rate`、`ff_delta_applied_total`、§4 两个留痕指标（标定框架的骨架）
3. 日志事件 schema（space 粒度明细的结构定义——离线标定指标的原料）

**可延后的**：§2.2/§2.3 的离线 gauge（种子期前补上离线聚合任务即可）；cell_watermark（§17 Cell 架构落地时随行）。

## 7. 待决项

1. 各 FF 标定指标的"健康区间"——本质是参数标定的一部分，种子期第一轮数据后回填
2. 离线聚合任务的运行载体（轻量定时 worker 即可，不引入新组件；频率初值日更）
3. Prometheus 单点的 HA 诉求——1.0 可接受监控自身单点（监控挂了不影响业务），2.0 服务商场景再议

---

*本稿为 §10"可观测性埋点设计"立项任务的产出，评审通过后以新版本号并入主文档（§10 勾除），并作为 1.0 开发期的实现依据。*

# M14 SS 打分 LLM 客户端配置（OpenAI 兼容端点）
# 复制为 .env 并填入真实值；.env 永不入库（.gitignore 已排除）
SS_LLM_BASE_URL=
SS_LLM_API_KEY=
SS_LLM_MODEL=

# M15 写入链 embedding 客户端配置（OpenAI 兼容 /embeddings 端点）
# 注意：DeepSeek 无 embeddings 端点，需另配 provider；共享 rms_vectors 必须同模型
# 同维度——DIMS 必须等于索引 mapping dims，模型变更 = 向量全量重建（修订记录 23 条④）
LETHEFIELD_EMBED_BASE_URL=
LETHEFIELD_EMBED_API_KEY=
LETHEFIELD_EMBED_MODEL=
LETHEFIELD_EMBED_DIMS=

# 课题 — dsh 会话日志与 EX 事件流映射 v0_1

**版本**: v0.1
**状态**: 待研（配合《规划-DeepSeekHarness集成-认知插件-v0_1》T1/T4）
**日期**: 2026-08-23

---

## 0. 课题

dsh（DeepSeek Harness）的运行时不变量为 **Model-Visible ⟺ Logged**：模型可见的一切上下文必须可从会话事件日志重建。该不变量与 Lethefield EX（全量事件记录、不可变 source of truth）的定位几乎同构。本课题回答：dsh 会话事件流在多大程度上可以直接充当 EX 的摄入源？哪些 Lethefield 机制可原生运行其上，哪些需要适配或降级？

## 1. 概念映射初表（待核实，基于公开文档）

| dsh 概念 | Lethefield 对应 | 初判 |
|---|---|---|
| 追加写会话事件日志（append-only session log） | EX 全量事件记录 | 同构；事件 schema 映射待 T4 落实 |
| 上下文注入插件 seam | RMS retrieve（塌缩的工程版） | 同构；同步注入路径，延迟敏感 |
| 会话恢复 / 分叉 / 回放（resume / fork / replay） | EX 事件流 + 事件视界距离度量 | 加强项：fork/replay 天然产生"经历的事件数"序列，是事件视界距离的理想数据源 |
| Capability Seam 的 Provider | Lethefield 整栈（api/fs/rms/scheduler） | 一个 Provider 替换改变整条认知链 |
| workspace / session | space（租户隔离单位） | 需一对一映射；隔离红线见规划文档 §3 |

**初步判断**：dsh 替 Lethefield 免费解决了认知服务最难的数据采集问题——EX 摄入管道不必自建，dsh 的会话日志即事件流；FF 三重动力学（尤其事件视界距离）可直接运行于其上。

## 2. 待决问题

1. **Q1 事件粒度**：dsh 日志事件（消息 / 工具调用 / 工具结果 / 注入记录……）与 EX 事件模型的粒度差异？是否存在 EX 需要而 dsh 不记录的事件类型（如检索行为自身的留痕）？
2. **Q2 显著性六维的数据支撑度**：SS 的情绪 / 新颖性 / 目标 / 冲突 / 重要性 / 显式请求六维，各有几个可从 dsh 事件字段直接推导？缺口维度首版是否降级（R3 预案：仅启用事件视界距离 + 墙钟衰减）？
3. **Q3 fork 的语义**：dsh 会话分叉在 Lethefield 侧应建模为 space 内事件流分叉，还是新 space？前者需要 EX 支持分支指针，后者更干净但切断分叉前记忆的连续性。
4. **Q4 回放与遗忘的相互作用**：dsh replay 重放历史事件时，FF 是否应将回放计入"经历"（事件视界距离推进）？直觉上回放是重新暴露、应刷新可见度——需对照 3.0 蓝图的事件视界定义确认。
5. **Q5 注册阈值**：dsh 高频低信息事件（心跳类工具结果、重复 lint 输出）是否触发背景生成（预测误差归零不注册独立向量）？这是"背景生成"机制的第一个真实数据源。

## 3. 产出要求

- 事件字段映射表（dsh event schema → EX event schema，逐字段）
- 六维显著性支撑度评估表（每维：可推导 / 需插件补采 / 不可行）
- Q3 / Q4 的建模决策建议（提交设计评审）

## 4. 依赖

- dsh 插件开发文档与 Cordis 公开 seam 清单（规划 T1 的产出为本课题输入）
- 《研究-认知动力学-3.0蓝图-v0_1》：事件视界距离、背景生成的定义核对

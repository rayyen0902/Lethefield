# Lethefield 种子期计划 v0.1

- 日期：2026-08-18
- 前置状态：阶段 1 技术落地验证已准出（2026-08-15，tag `stage-1-exit`，`准出评审报告-v1_0.md`）
- 本文档定位：种子期（阶段 1 → 阶段 2 之间）的唯一计划入口；参数标定结果回填《Lethefield-开发文档》§20，设计层变更走升级确认 + 修订记录老流程，不在本文档单方面定案
- 待定项一律标 **【待拍板】**，由用户补充后升版本

---

## 1. 目标与准出标准

种子期的使命：用真实用户流量完成参数标定与运营闭环验证，证明 1.0 系统"不仅跑得通，而且记得住、想得起来"。

准出标准（全部满足才可启动 2.0 商业化落地）：

- [ ] 种子用户在线运行 **【待拍板：目标人数与运行时长，建议 ≥10 人 / ≥4 周】**
- [ ] 真实事件量级 **【待拍板：建议人均 ≥100 条，总量 ≥1000 条】**
- [ ] §20 全部占位参数完成标定回填（λ、θ_base、grace_n、SS 六维权重、W、W_r3、配额上限、水位阈值等），每个参数有标定依据记录（数据来源 + 决策留痕）
- [ ] **car_new 基准复验通过**：事实型偏好记忆（车、地址、口味）在 Δn=0 与长时间跨两种场景下均可召回（修订记录：种子期标定第一优先级基准，`deploy/baselines/smoke_real_llm_v1.md`）
- [ ] 混沌演练 R1 首轮 7 场景全部执行，告警空白 6 条有处置结论（补齐或明确豁免）
- [ ] 决策留痕库有真实运营决策积累（非测试数据），留痕库独立持久卷 + 备份策略落地
- [ ] 无 P0 级数据安全事件（跨 space 泄漏、未授权训练数据入管线、销毁残留）

## 2. 前置工程（种子用户接入前必须完成）

| 项 | 内容 | 依据 |
|---|---|---|
| E1 | 决策留痕库独立持久卷 + 备份策略（`make reset` 不得波及留痕库；补纪律：留痕库永不 reset） | 准出评审闭合项 1 根因（id=1 记录被清卷丢失） |
| E2 | 观察项 7 条消化（M2 temporal 负向测试 / M7 默认形参对齐 / M9 演练记录 / M10 DMS 第 4 路集成测试 / M13 三项 / M17 记录与工作日志 / M0 排版） | `准出评审报告-v1_0.md` 观察项汇总 |
| E3 | 混沌演练 R1 首轮（7 场景，物理机 ubunturay；**执行前 BIOS 恢复默认频率，关闭超频**） | `规划-混沌工程测试计划-v0_1.md` §0 |
| E4 | 告警空白 6 条处置（API 5xx 计数 / worker 活性 page 出口 / 积压 backlog 告警 / DLQ 重放工具 / PG 故障信号 / 归档重放保真实测） | 混沌计划 §4 |
| E5 | 物理机 sshd 安全核实（`PasswordAuthentication no` + `PermitRootLogin no`，建议 fail2ban；公网通道 `ssh -p 6000 ray@39.106.89.255` 暴露面） | `环境-物理机-ci-runner-v0_1.md` §2 |
| E6 | 向量侧静默全灭的可观测性（金丝雀查询探针或等价手段，区分"无相关记忆"与"检索面死亡"） | 工作日志已登记缺口（2.0 候选，种子期先评估） |

## 3. 运营准备

- [ ] **用户协议授权条款成文**：1.0 阶段训练数据全部接入且数据在我方（已定案），协议须包含训练数据授权条款；文本属信托/宪法层面产物，本仓库只挂完成状态
- [ ] **种子用户画像与招募渠道【待拍板】**：建议优先技术背景用户（容忍度高、反馈质量高）；C 端伴侣场景与开发者场景各占比【待拍板】
- [ ] **支持通道**：问题反馈入口、响应承诺、事故通报模板
- [ ] **许可证策略**：BUSL 类源码可用 vs 全开源——上社区宪法层面讨论（信托议题，不属工程执行范围，但种子期对外露出前必须有结论）【待拍板】。
  **状态（2026-08-23 所有者裁定）：记为待办，暂缓**——LICENSE 选型需深度探讨与思考，当前状态不足以决策。注意派生后果：贡献者任务池开放以此项落地为硬前置（ LICENSE + DCO 未定前不接受外部 PR，授权链风险见留言板 §5.4R 意见 4a），任务池保持关闭。

## 4. 参数标定计划

标定顺序（有依赖关系，按序执行）：

1. **SS rubric 与六维权重**——事实型记忆系统性低分问题（mean 0.298）优先修；基准 = car_new 案 Δn=0 可召回；手段：rubric 修订（设计层升级确认）→ 权重标定（`LETHEFIELD_SS_WEIGHTS_JSON`）
2. **θ_base**——随 SS 分布标定后定，保证事实型记忆不被误滤、噪声仍可滤
3. **λ（衰减率）**——有效记忆窗目标【待拍板：建议 ≥50 事件窗】，以真实使用节奏回推
4. **grace_n / N_neglect / 固化阈值**——在 λ 定案后标定
5. **W（DMS 窗口）/ W_r3（R3 关联窗）**——运营节奏数据回填
6. **配额上限 / 水位阈值 70/90 / Cell 容量**——资源实测回填

纪律：标定依据必须留痕（数据来源 + 决策留痕 submit）；每次参数变更走配置，不改代码；标定后 car_new 基准与冒烟 7 查询集必须复验回归。

## 5. 风险与退出条件

- **R1 打分成本失控**：真实流量下 LLM 打分成本超预期（冒烟 ≈370 tok/事件）——阈值【待拍板】，超阈值评估蒸馏小模型（物理机 A4000，2.0 候选提前）
- **R2 检索质量标定失败**：rubric + 权重 + θ 三轮迭代后 car_new 类基准仍不过 → 升级设计层，评估 SS 方案本身（换模型/改六维定义）
- **R3 数据安全事件**：任一 P0 事件 → 种子期立即暂停，回到工程修复
- **退出条件（失败定义）**：连续 4 周活跃用户 < 目标的 30%，或 R2 触发且设计层评估无解 → 种子期终止，复盘后决定 2.0 是否继续
- **2.0 提前启动信号**：种子用户需求集中指向服务商部署/多 Cell 商用形态 → 提前启动 2.0 规划（第三方服务商部署规范、per-space 加密、商业化 tier）

---

*v0.1 骨架由参谋会话起草（2026-08-18），【待拍板】项待用户补充后升 v0.2。*

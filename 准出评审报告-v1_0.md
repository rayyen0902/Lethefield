# Lethefield 阶段 1 准出评审报告 v1.0

- 日期：2026-08-14
- 依据：《Lethefield-开发文档》v1.2 §19 验收总览（10 条准出条件）+ 各模块"验收标准"小节
- 方法：逐条核对，证据只认仓库内实际存在的测试用例 / 脚本 / 报告 / 留痕记录；无证据如实标缺口
- 评审基线代码：HEAD = `2348e4a`（物理机移机复验）

---

## 条件 1：M0 工程地基验收通过 —— **过**

| 验收项 | 证据 | 缺口 |
|---|---|---|
| 一键起栈 + CI 全绿 | `Makefile`（up/reset/lint/test/itest/ci）；`scripts/ci.sh`（lint→单测→红线静态扫描→起栈→集成测试）；`docker-compose.yml`（Cassandra×2、ES×2、JanusGraph、Pulsar、Redis AOF、Postgres，cell2 profile 按需） | 无 |
| 物理机验收 + 移机复验（两次实证） | `环境-物理机-ci-runner-v0_1.md`：§3.5 首验（ubunturay 100.92.236.89，全量 CI 8m35s 全绿，单测 505 passed / 集成 122 passed / 1 skipped）；§6 移机复验（2026-08-14，同口径复跑全绿，426.81s ≈ 基线 422.21s）；commit `22a4b26`、`2348e4a` | 文件 §5 缺口清单编号重复（排版瑕疵） |
| 共享库三样独立测试 | `libs/logschema/tests/`、`libs/metrics/tests/`、`libs/clients/tests/`（13 个测试文件） | 无 |
| spike q1–q4 转 CI 基线 | `spike/SPIKE_REPORT.md`；`tests/integration/test_q1_stack_graph.py` / `test_q2_vector.py`（含 `test_routing_isolates_other_space`）/ `test_q3_graph_open.py` / `test_q4_business_loop.py::test_business_loop_four_assertions` | 无 |
| 决策留痕可提交可查询；授权注册表增删查 | `ops/decision_log`（submit/get/list）；`tests/integration/test_ops_minimal.py::test_decision_log_submit_and_query`；`libs/clients/auth_registry.py`（六操作）+ `libs/clients/tests/test_auth_registry.py` | `ops/decision_log` 无独立单测目录（集成层覆盖，薄弱非缺失） |

## 条件 2：M1–M10 各模块验收标准全部通过 —— **过**（M10 准出档 2026-08-14 已闭合，见文末闭合记录）

### M1 存储基础设施 —— 过

- 物理隔离证明：`scripts/verify_isolation.py`；`test_m1_infra.py::test_physical_isolation`
- ids.authority.wait-time 巡检：`scripts/check_graph_config.py`；`test_m1_infra.py::test_ids_authority_wait_time_default`
- 时钟跳变告警：`ops/clock_monitor`；`test_m1_infra.py::test_simulated_clock_jump_triggers_alert`（注入 +60min）
- 重启基线：`scripts/measure_restart_baseline.sh` + `deploy/baselines/restart_baseline.jsonl`（2026-08-04 实测 8 组件）

### M2 RMS 图 Schema —— 过

- 17 属性键 + 索引巡检：`scripts/check_rms_schema.py`；`test_m2_schema.py::test_full_schema_landed`；单点常量 `lethefield_rms/schema.py`
- 五类边可建：`test_m2_schema.py::test_five_edge_types`；temporal immutable 靠"边无属性键 + 衰减只触节点"结构性保证（`schema.py:43`、`writer.py:188`）
- 向量零泄漏：`test_m2_schema.py::test_vectors_zero_leak`（routing + term 双机制）
- ref_ex 追溯：`test_m2_schema.py::test_ref_ex_sampling`
- 缺口（观察项）：temporal 边 immutable 无负向测试

### M3 FF 引擎 —— 过

- 边界单测：`services/rms/tests/test_ff.py`（`test_delta_n_zero_is_no_decay` / `test_lambda_zero_is_no_decay` / `test_matches_hand_calc` / clamp 四例）
- 端到端 θ 过滤：`test_m4_retrieve.py::test_end_to_end_q4_decay_scenario`、`test_m3_ff.py::test_decay_filter_scenario`
- s 只在 δ 触发时变化：`test_m3_ff.py::test_s_only_changes_at_delta_trigger`
- clamp 指标：`test_m3_ff.py::test_clamp_metric_upper/lower`

### M4 检索流程 —— 过

- 带边子图：`test_m4_retrieve.py::test_returns_edged_subgraph`
- Stage 3 无 ES 调用：`test_m4_retrieve.py::test_stage3_makes_no_es_calls`（调用计数断言）+ `_stage3_traverse` 签名物理隔离
- supersedes 重定向：`test_m4_retrieve.py::test_supersedes_redirect`（默认返回 B，`trace_history=True` 返回 A）
- ρ 只影响硬过滤：`test_m4_retrieve.py::test_rho_only_affects_hard_filters`
- 跨 space 隔离：`test_m4_retrieve.py::test_cross_space_isolation`

### M5 接口层 —— 过

- 等 EX ack 返回：`test_m5_api.py::test_record_returns_after_ex_ack` 等
- actor 伪造拒止：`test_m5_api.py::test_actor_spoof_rejected`（400 + 零写入断言）
- φ 字段裁剪：`test_m5_api.py::test_retrieve_default_crops_ff_fields` / `test_retrieve_debug_scope_exposes_phi`（字段级对照）
- reinforce 无 consolidation 路径：`test_m5_api.py::test_reinforce_has_no_consolidation_path`
- 限流挂载点：`test_m5_api.py::test_rate_limiter_mount_point`

### M6 FS —— 过

- 无边推断逻辑：`grep addE|create_edge services/fs/src` 零命中（结构性）
- 忽视幂等：`test_m6_fs.py::test_neglect_idempotent_per_interval`
- 归档/固化独立路径：`test_m6_fs.py::test_archive_end_to_end` / `test_consolidate_end_to_end`；物理删除按定案不在 FS（归 M9/M10 destroy，已有 `test_destroy_*` 覆盖）——口径确认项，非缺口
- sweep DMS：`lethefield_fs/liveness.py`；`test_m6_fs.py::test_worker_run_once_and_liveness`
- 元事件不推进 n：`test_m6_fs.py::test_meta_event_does_not_advance_n`

### M7 纠错机制 —— 过

- 无失效标志字段：`test_m7_corrections.py::test_schema_has_no_invalidation_flags`
- EX 重放完整重建：`test_m7_corrections.py::test_rebuild_from_ex`；单测 `services/rms/tests/test_rebuild.py` 15 例；全保真档 `ex_scoring_s_resolver` 三用例（`test_rebuild.py:235/244/254`）；详见条件 8
- 链式纠错 A→B→C：`test_m7_corrections.py::test_chain_correction_retrieval`
- reinforce 时间窗合并：`test_m7_corrections.py::test_reinforce_window_merge`
- 重复 flag_conflict 幂等：`test_m7_corrections.py::test_flag_conflict_processed_idempotently`
- 缺口（观察项）：`rebuild.py:179` 函数签名默认形参仍为 `placeholder_s_resolver`，全保真档默认切换在 CLI 入口层——按"默认"字面可要求对齐

### M8 空间模型 —— 过

- agent_id 残留扫描：`scripts/check_space_model.py`（已接 ci.sh）
- 多 actor 共享：`test_m8_space_model.py::test_multi_writer_share_ex` / `test_multi_writer_share_rms`
- 核心服务无 space_type 分支：`check_space_model.py` 静态强制

### M9 Cell 架构 + 调度器 —— 过

- 映射表备份/导出：`libs/clients/control_backup.py`；演练 `test_m9_cell_scheduler.py::test_backup_restore_drill`（落盘往返）
- 开通顺序 + 回滚：`services/scheduler/tests/test_provision.py::test_happy_path_order` / `test_graph_step_failure_rolls_back_storage` / `test_register_failure_rolls_back_all`；集成 `test_m9_cell_scheduler.py::test_provision_storage_failure_rolls_back`
- 注销先驱逐后删存储：`test_m9_cell_scheduler.py::test_destroy_evicts_then_drops_no_residue`；`test_destroy.py::test_destroy_order_close_before_drop`
- 调度器下线已有 space 仍工作：`test_m9_cell_scheduler.py::test_scheduler_outage_existing_space_read_write`
- 水位转换门控分配：`test_m9_cell_scheduler.py::test_watermark_transitions_gate_allocation`
- 缺口（观察项）：备份恢复无 `deploy/baselines/` 独立演练记录文件，证据仅 CI 测试

### M10 EX + Pulsar + 生命周期流水线 —— **过**（准出档 2026-08-14 闭合）

- EX/Cell 物理独立：`verify_isolation.py`（cassandra-cell:9042 vs cassandra-ex:9043）
- 开通回滚 / 注销顺序：与 M9 共用证据，过
- 注销第 4 步真实广播：`libs/clients/training_control.py`（真实 Pulsar 发布等 broker ack）；`test_m10_destroy_broadcast.py::test_destroy_broadcast_reaches_training_sink` / `test_broadcast_failure_aborts_then_retry_completes`
- DMS 主动探测：`ops/ingest_dms` 四路；集成 `test_m10_ingest_dms.py` 四用例（探针/backlog 停滞/新鲜度）；第 4 路 n 一致性仅单测（`ops/ingest_dms/tests/test_n_consistency.py`，观察项）
- 迁移演练本地档：`deploy/baselines/m10_migration_drill.jsonl`（2026-08-07，只读窗口实测 15.6s/16.1s，预算 60s），过
- **迁移演练准出档：已执行（2026-08-14，物理机 ubunturay）。** `deploy/baselines/m10_migration_drill_exit.jsonl`（2 条记录，两连跑全绿）：compose `ex2` profile 构成真实双 EX 集群（cassandra-ex-2:9143），完整流水线含 **EX 跨集群 sstableloader 流式传输**（nodetool snapshot → `sstableloader -d cassandra-ex-2` 逐表流式，经 `migrate_space` 新增 `ex_transfer`/`to_ex_cluster_id` 成对注入点进入流水线）；实测只读窗口 21.251s/20.969s（预算 60s），EX 传输 26980B/10.383s 与 26973B/10.598s，迁移后校验全过（EX 行数一致、图遍历正常、n 连续性 Redis==目标 MAX(n)）；演练可重跑 `tests/integration/test_m10_migration_drill_exit.py`。known_gaps 仅剩 API 层多 Cell 连接路由（M9 遗留，如实保留）。

## 条件 3：M14 / M15 / M16 验收通过（含 SS 稳定性报告入决策留痕） —— **过**（M14 留痕 2026-08-14 已补录，见文末闭合记录）

### M14 SS 打分 —— 过

- 端到端可追踪 + 权重来自配置：`test_m14_ss.py::test_record_to_scoring_end_to_end`（六维 + 合成 s + model_version 字段级）、`test_weights_from_config_change_s`；真实 LLM 链路 `deploy/baselines/smoke_real_llm_v1.md`（6/6 冒烟）
- 失败 → DLQ + 告警 + 不丢不重：`test_m14_ss.py::test_scoring_failure_retries_then_dlq` / `test_duplicate_delivery_no_duplicate_write`
- 稳定性报告：`deploy/baselines/m14_ss_stability.json`（deepseek-v4-flash，50 样例 50/50 成功、0 degraded、六维无塌缩，s mean 0.365 / p50 0.425）；样例集 `services/ss/samples/stability_samples.jsonl`
- 单维缺失降级：`services/ss/tests/test_scoring.py`（`test_score_event_one_missing_neutral_mark` 等五例）+ `test_prompt.py` 解析矩阵
- **缺口已闭合（2026-08-14）**：稳定性结论留痕已补录，当前可查询——`lethefield_decision_log get 14`（标题「M14 SS 稳定性验证结论：LLM prompt 六维打分路径可用，权重维持均权占位待标定」，agent_suggestion 引用 `deploy/baselines/m14_ss_stability.json` 结论数据；context 如实注明补录缘由）。实查输出已贴 `工作日志.md` 闭合小节。

### M15 写入链 —— 过

- 端到端字段级（含 φ 初值）：`test_m15_writer.py::test_record_to_graph_end_to_end`
- 幂等（重复投递 3 次 → 2 顶点 1 时序边无重复向量）：`test_m15_writer.py::test_duplicate_delivery_single_vertex_edge_vector`；部分失败补全 `test_writer_worker.py::test_partial_failure_repair`
- 元事件不建顶点不推进 n：`test_m15_writer.py::test_reinforce_meta_event_no_vertex`
- 向量一一对应 + 跨 space 隔离：`test_m15_writer.py::test_cross_space_vector_isolation`（routing + term 双机制）

### M16 IS —— 过

- 三级关系 + claim 结构：`test_m16_is.py::test_three_level_relation_and_claim_structure`（四字段 + jti/exp/iat，与吊销列表行核对）
- 创建触发流水线 + 失败无半开通：`test_m16_is.py::test_provision_failure_rolls_back_without_ownership`
- 吊销即拒：`test_m16_is.py::test_issued_token_accepted_then_revoked_rejected`（真实 API 200 → revoke → 401）
- C 端 debug 拒签 + 内部 debug 取回 φ：`test_m16_is.py::test_c_end_debug_scope_rejected` / `test_internal_debug_token_exposes_phi`

## 条件 4：M17 运维操作面验收通过 —— **过**

- CLI 覆盖全部人工触发点：`ops/ops_cli/src/lethefield_ops_cli/commands.py` 九个 `cmd_*` 逐条存在；集成 `test_m17_ops_cli.py`（`test_status_and_set_tier` / `test_auth_revoke`（含真实存量 scrub）/ `test_cell_watermark_and_register` / `test_destroy_end_to_end`）；迁移三类集成层测组装逻辑，真实迁移归 M10 drill（测试 docstring 明示此划分）
- 静态检查：`ops/ops_cli/tests/test_no_global_commands.py`——parser 内省断言叶子命令集合**精确等于**九条清单、每条必选 `--space(s)`/`--cell` 绑定
- 留痕可查：`lethefield_ops_cli/audit.py::run_with_audit`；`test_ops_audit.py`（PG 预检 fail-closed / 退出码 2 分支）；集成 `test_m17_ops_cli.py::test_audit_fail_closed`（真实栈坏 DSN → 拒绝执行 + 零副作用）
- 销毁处置端到端演练：**真做过端到端**。`test_m17_ops_cli.py::test_destroy_end_to_end` 走真实 `__main__.main`（与人工执行同路径）→ 真实 gremlin/EX/Pulsar 连接调 `destroy_space`（契约 5 真实广播，非 mock）→ 断言映射无残留 + decision_log 留痕齐全；前置真实双 space + 图顶点/边/向量 + 授权热层样本。物理机全量 CI（8m35s）含本套件
- 缺口（观察项）：无独立演练记录文件（对比 M10 有 drill jsonl）；`工作日志.md` 无 M17 完成小节

## 条件 5：M11 训练管线最小集完整可用 —— **过**

- 入料口①：`ops/decision_log --feed`（R1/R2 命中才发布）；`test_m11_training_pipeline.py::test_r1_r2_from_decision_submit`（含常规流量不产样本）
- 入料口②：`submit-incident`；`test_incident_becomes_r5_sample`
- 入料口③：`lethefield_training.recall_filter`（es-ops 轮询 → 授权闸门 → feeds/raw）；`test_recall_filter_gate`（未授权拦截/grant 转发/revoke 再不转发三态对比）；worker 侧第二道防线 `test_worker_drops_unauthorized_feed`
- 入料口④（超出最小集要求，已落地）：`ex-feed`；`test_ex_feed_from_real_ex`（CONTENT_COPY 闸门）
- R1–R3：`decision_rules` 纯函数单点（`training_feed.py:71`）；R3 关联 `test_r3_correlation_and_miss`（含 miss 语义边界 + W_r3 窗 + 跨 space 不关联）
- 热层落盘：`hot_store.py`（`hot/samples-日期.jsonl` + `index/{space_ref}.jsonl`）
- 撤回/销毁联动：`test_revoke_then_scrub_via_manifest`（O(清单) 定位 + 幂等）；`test_destroy_command_consumed_and_processed`（真实广播 → scrub + 注册表删除 + 留痕）
- worker 无业务库直连：`services/training/tests/test_no_business_db.py`（AST 静态强制）

## 条件 6：M12 开发期最小集指标全部可查询 —— **过**（逐项查询验证 2026-08-14 已闭合，见文末闭合记录）

- 15 项指标 + 日志事件 schema 全部有实现（定义点逐一核实：`retrieve.py` / `service.py` / `ex_ingest.py` / `worker.py` / `backlog.py` / `freshness.py` / `exporter.py` / `aggregations.py`；schema `libs/logschema/events.py` extra=forbid），其中 12 项有测试覆盖（`test_m12_observability.py` + 单测）
- 标签黑/白名单代码层强制：`libs/metrics/registry.py`（`LABEL_BLACKLIST={"space_id","node_key"}`）；`test_registry.py::test_blacklisted_labels_rejected`
- 聚合只读日志管线：`exporter.py:175` `@redline1_exempt` 登记 + check_space_filter 规则 B 静态约束
- 埋点与训练管线物理分离：`services/api/src/` 全目录 grep 无训练发布残留；收口链路 `test_m12_observability.py::test_recall_filter_chain`
- Grafana 面板存在：`deploy/grafana/provisioning/dashboards/lethefield.json`（6 面板含 PromQL）+ datasource provisioning + `deploy/prometheus/prometheus.yml`（7 job）
- **缺口已闭合（2026-08-14）**：逐项查询验证已执行并留记录——`scripts/verify_metrics_queryable.py`（可重跑）对 16 个指标名逐项真实 Prometheus `/api/v1/query` 按名断言 + 类型比对（`/api/v1/metadata` vs 源码定义），并执行 Grafana 面板全部 6 条 PromQL；结果 `deploy/baselines/m12_metrics_queryable_v1.md`：**16/16 有真实数据且类型一致、6/6 PromQL 有序列，无一跳过**。3 个无按名断言指标已补测试：`lethefield_fs_sweep_processed_total`/`lethefield_fs_sweep_lag_seconds`（`test_m6_fs.py::test_worker_run_once_and_liveness`）、`lethefield_pulsar_backlog_events`（`test_m10_ingest_dms.py::test_backlog_stall_triggers_page_alert`）。

## 条件 7：M13 六条红线均有自动化检查手段 —— **过**

- 红线 1：`scripts/check_space_filter.py`（AST 三规则 + `@redline1_exempt` 登记制 + 入口 --space 收敛口），接 ci.sh:20-21
- 红线 2：`lethefield_rms/quota.py` + writer 三原语强校验；真计数触发 `test_m13_redlines.py::test_quota_rejects_beyond_limits`；429 映射 `test_http_quota.py::test_quota_exceeded_maps_to_429`；`max_returned_nodes` 测试在
- 红线 3：`sweep_due` 纯函数 + tier 矩阵单测；Redis AOF（compose:174-179）+ `check_redlines.py::check_redis_exemption`；n 一致性巡检 `n_consistency.py` + 单测
- 红线 4/5/6：`scripts/check_redlines.py`（红线 4 静态 / 红线 5 位置比对 / 红线 6 + `--runtime`），接 ci.sh:23-24；`test_m13_redlines.py::test_check_redlines_runtime_passes` 调起 runtime 档
- 缺口（观察项，不阻塞）：① `QuotaCounters.vector_count` 双机制（routing + space_id term）的串计数回归测试未固化（`test_quota.py:145` 只断言 routing，AGENTS 记载的"27 条串计数实测"无对应测试）；② `check_redlines.py` 静态比对逻辑本身无单测；③ metrics_exporter 无 training worker 那样的禁 import AST 测试（靠登记 + 规则 B）

## 条件 8：EX 重放重建脚本可执行且通过校验 —— **过**

- 报告存在：`deploy/baselines/rebuild_fidelity_v1.md` + 可重跑脚本 `scripts/rebuild_fidelity_drill.py`（三相位）
- 零差异结论（原文）：顶点字段级 **20/20 精确相等，零差异**；temporal 边 19 + supersedes 边 2 完全一致；被纠错旧节点重放后 s=0.000 与销毁前精确一致；RTO 21.9s（20 事件微型 space）
- 数据源为真实冒烟（DeepSeek 真实打分 + text-embedding-v4 真实向量）；**s 全保真档**（`ex_scoring_s_resolver`，无 `rebuild_scoring_missing`）
- 修订记录第 25 条边界：rms_vectors 不属重放范围（检索加速层，ES 快照为运维前提）——演练实测实证：rms_vectors 销毁后 retrieve 静默全灭，直到 ES 快照恢复；node_key 关联语义可重建性已验
- 修订记录第 27 条边界：冲突 δ touch 时刻取 `event.n` 事件时刻语义，直播与重放天然一致，保真校验精确相等无容忍例外（commit `02fd247`）
- 缺口（已自登记，归混沌演练）：归档重放保真未覆盖（数据集 n=20 < n_star+grace_n=60）

## 条件 9：混沌工程测试计划已制定 —— **过**

- 计划存在：`规划-混沌工程测试计划-v0_1.md`（2026-08-14），自我定位"只写计划不演练"
- 故障场景清单 7 条（Pulsar / Cassandra-ex / Cassandra-cell / ES-graph 含删卷 / Redis FLUSHALL / writer·SS 崩溃 / 时钟偏移 / PG 不可用），每条含注入手段、预期告警（指到已落地指标/日志事件名）、自愈预期、恢复校验
- 演练节奏：准出前 7 场景全量一轮（记录入 `deploy/baselines/chaos_drill_r1.md`）；种子期每月轮换 1–2 场景
- 告警空白 6 条已在计划 §4 定性登记（API 5xx 无计数、worker 活性无 page 出口、ex-events/scoring-results 积压无 backlog 告警、DLQ 无独立重放工具、PG 故障无 page 信号、归档重放保真未实测）——**按既有定性归执行期事项，不构成准出缺口**
- 说明：`chaos_drill_r1.md` 实演记录尚不存在，符合"计划存在即过"的判定口径

## 条件 10：§11.3 决策留痕机制跑通 —— **过**

- 机制可用：`DecisionLogStore` submit/get/list + R1/R2 判定发布（`store.py:124 _feed`）+ ops_cli 全命令自动留痕包装；三列迁移 `deploy/postgres/migrations/001_decision_log_m11.sql`（幂等，含 CHECK 约束）
- 当前库实查：6 条记录（outcome 覆盖 accepted/rejected/modified，escalation_type 有 cross_space/novel_error 样本），但**均为 2026-08-14 集成测试/联调产生的发夹数据，无真实开发决策积累**
- 判定：§19 原文"不要求已积累数据，要求机制可用"——满足字面要求（查询命令备查：`docker compose exec -T postgres psql -U lethefield -d lethefield -c "SELECT ... FROM decision_log"`）

---

## 已知背景（如实纳入，不算新问题）

- **标定观察项三条归种子期**：真实冒烟检索明细 7 查询 3✓4✗（`deploy/baselines/smoke_real_llm_v1.md`），连同 §20 全部待标定参数（λ/θ_base/W_r3/SS 权重/水位阈值等）均为占位值待种子期数据回填
- **Mac 侧 Cassandra 慢化 flake 归环境已知项**：物理机 CI 全绿（8m35s / 复验 426.81s）为准出口径，Mac dev 机抖动不入判定
- **告警空白 6 条归执行期**：已在混沌计划 §4 登记定性（见条件 9）

## 观察项汇总（不阻塞准出，建议随执行期消化）

1. M2：temporal 边 immutable 无负向测试
2. M7：`rebuild.py:179` 签名默认形参与 CLI 默认档对齐
3. M9：备份恢复补一份 `deploy/baselines/` 演练记录
4. M10：DMS 第 4 路 n 一致性补集成测试
5. M13：`vector_count` 双机制串计数回归测试固化；`check_redlines.py` 静态逻辑加单测；exporter 补禁 import AST 测试
6. M17：销毁演练补独立记录文件；`工作日志.md` 补 M17 完成小节
7. M0：`环境-物理机-ci-runner-v0_1.md` §5 编号重复排版修正

---

## 总结论：**有条件准出** → 三项条件已全部闭合（2026-08-14，见下方闭合记录），待评审人官宣

§19 十条准出条件中，7 条通过，3 条有条件通过。闭合以下三项条件后可官宣准出：

1. **M10 迁移演练准出档**（条件 2 唯一未满足项）：在临时扩容环境补跑一次含 **EX 跨集群 sstableloader 流式传输**的完整迁移演练，实测数据入 `deploy/baselines/`。这是开发文档 M10 验收标准的明文要求，不能豁免。
2. **M12 指标逐项可查询验证**：对开发期最小集 15 项指标执行全量 Prometheus 查询验证（含 Grafana 6 面板 PromQL 执行）并留记录；顺带补 3 个无按名断言指标的测试。
3. **M14 稳定性结论留痕补录**：原 id=1 记录已随 dev 卷 reset 丢失，用 `lethefield_decision_log submit` 补录一条（标题即工作日志所记内容），使"结论进决策留痕"当前可查询。

无其他阻断项。本报告仅为评审材料，准出官宣由评审人决定。

---

## 条件闭合记录（2026-08-14 补录）

三项条件全部闭合，证据指针如下；全量 CI 结果见末节。

1. **M10 迁移演练准出档 → 过**：物理机 ubunturay 临时扩容环境（compose `ex2` profile 第二 EX 集群）两连跑全绿；实测数据 `deploy/baselines/m10_migration_drill_exit.jsonl`（2 条）；可重跑演练 `tests/integration/test_m10_migration_drill_exit.py`；流水线注入点 `services/scheduler/src/lethefield_scheduler/migrate.py`（`ex_transfer` + `to_ex_cluster_id` 成对）。
2. **M12 指标逐项可查询验证 → 过**：`scripts/verify_metrics_queryable.py` + 记录 `deploy/baselines/m12_metrics_queryable_v1.md`（16/16 有数据且类型一致、6/6 PromQL 有序列、零跳过）；3 个补测落在 `test_m6_fs.py` / `test_m10_ingest_dms.py`。
3. **M14 稳定性结论留痕补录 → 过**：`lethefield_decision_log get 14` 可出示；实查输出贴 `工作日志.md` 闭合小节。

**全量 CI（物理机 ubunturay，2026-08-14）：绿。** `bash scripts/ci.sh` 复跑 **124 passed，0 failed，493.08s**（含两个迁移演练在 CI 内真实执行——compose profile 容器不受 `make reset` 影响保持在线，exit 档演练记录因此累计 4 条，只读窗口 19.3s–21.3s）。注：首轮 CI 未按惯例先 `make reset`（累积卷 + 演练 profile 同时在跑），出现 m8/m9/q3 族 Cassandra schema 操作超时（7 failed/5 errors，AGENTS.md 已记载的环境性抖动形态）；reset 后复跑全绿，确认与本次改动无关。

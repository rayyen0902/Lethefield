# Lethefield — 1.0 开发实现规划

**版本**: v0.1（提案，未定稿）
**状态**: 规划收口后的第一份开发侧文档——评审通过后作为 1.0 开发的执行依据
**关联**: Lethefield-RMS-设计文档 v1.7（全部定案章节）；spike/SPIKE_REPORT.md（技术基线）
**日期**: 2026-07-29

> **对齐说明（2026-07-29 补）**：本规划稿已被《Lethefield-开发文档》v1.1 吸收并取代为 1.0 开发执行依据。两处口径以开发文档 v1.1 为准：①**部署形态**——本文 §1"逻辑隔离（Cassandra `space_id` 分区键 + ES 共享 index + custom routing）"的表述已被 Cell 架构取代，最终形态为 per-space keyspace + 每 space 独立 mixed index（向量仍为共享 `rms_vectors` + custom routing）；②**Cell 时机**——本文 §4 M6 的条件里程碑表述被取代，定案为"代码按 Cell 最终形态实现（`ControlPlaneStore` 抽象），部署按最小规模起步（单节点）"。本文保留作为开发顺序与里程碑划分的推理记录，不再单独维护。

---

## 1. 1.0 开发范围

**建五个服务 + 横切层**（§1）：IS、EX、SS、RMS（含 consolidation worker）、FS。**不做**：MS（§6.1）、Growth（§1 挂起）、Skill（§6.2）、Cell 调度器产品化（§17/§18 属阶段 1，见 §4 M6 的条件定位）。

**部署形态**：§15.4 阶段 0——自有小规模集群、逻辑隔离（§7.4：Cassandra `space_id` 分区键 + ES 共享 index + custom routing）。单节点起步（spike 环境即雏形），不为 1.0 预先建设多集群能力。

**对外交付面**：§9 四个 MCP/SDK 接口（`record/reinforce/flag_conflict/retrieve`）——1.0 的"完成"以四接口全部可用且可观测为准，不以"服务代码写完"为准。

## 2. 开发顺序的依据

1. **依赖链决定主线**：EX 是一切之源（source of truth 先行，§18.1 开通顺序同构）→ SS/RMS 消费 EX → retrieve 依赖 RMS 有数据 → FS 依赖图有状态
2. **价值前置**：retrieve 是用户体验的核心，不能压到最后——M3 中段就交付可读链路
3. **风险前置**：SS 六维打分的实现方式（LLM 打分 prompt 工程）是最大质量不确定项，放在 M2 早验证；Q4 spike 已证明检索链路可行，风险低
4. **横切不单独占里程碑**：埋点、留痕、训练管线骨架随所属服务同行（§19.6/§12.4 最小集是各服务的完成验收条件，不是独立项目）

## 3. 工程地基（M0，先行一周量级）

- **Monorepo**：Python 统一（§7.5）；服务=进程边界，共享库只放三样：日志事件 schema（§19.6）、指标 registry 封装（§19.5）、Pulsar/存储客户端
- **单节点编排**：沿用 spike 的 docker-compose（Cassandra/ES/JanusGraph）+ 补 Pulsar、Redis；spike 脚本（q1–q4）改造为**集成测试基线**，CI 必跑
- **决策留痕表单**（§11.3）与**授权注册表**（§12.4）的最小实现——两者都是后续服务的依赖，不是事后补丁

## 4. 里程碑

### M1：IS + EX——"能写"（memory.record 可用）

- IS：账号/空间/写入者三级模型（§8），JWT 签发（claim：account_id/space_id[]/agent_actor_id/scope，§9.2）
- EX：摄入端点（同步落库确认后返回，§9.3-3）、Cassandra 事件表、Pulsar 事件 topic、Dead Man's Switch（backlog 监控 + 窗口零摄入告警，§7.5.1）
- 验收：`memory.record` 端到端（凭证→摄入→EX 落库→确认返回）；零摄入告警可触发；`record_confirm_duration_seconds` 有数

### M2：SS + RMS 写入链——"能记"（EX→SS→RMS 全通）

- SS：六维度打分 worker（Pulsar consumer；LLM 打分 prompt 工程为**本里程碑最大风险项**，早验证、输出打分分布供 §19.2 标定）
- RMS：图写入（Event-Node φ_i 完整字段 §4.1 + 时序边）、n 序号分配（§13.2 Redis n_now）
- consolidation worker 骨架：语义边、n_star_cached 刷新（§4.4）；因果/实体边可简化为占位（标注 §4.2 完整实现留迭代）
- 验收：record 后异步可见图节点（含 s 初值、时序链）；`ff_delta_applied_total` 有数；打分失败有死信与重试

### M3：RMS 检索 + FS——"能用"（四接口齐全 = 1.0 功能完整）

- retrieve：§5 四阶段（ES kNN 锚点 routing → 1 跳子图 → FF 现算 s_effective → θ 硬过滤 + supersedes 重定向）；debug scope 附 φ_i（§9.3-2）
- reinforce 旁路（同步改 φ_i + 异步 EX 元事件，§13.7）；flag_conflict 事件化入链 → consolidation 建 supersedes 边 + −0.5（§13.7）
- FS：sweep（忽视惩罚 −0.1 按区间幂等 §13.3）、归档/固化（§13.4）
- 验收：四接口 MCP 调用全通；**Q4 spike 断言移植为回归测试**（高分召回/低分过滤/衰减过滤/跨 space 隔离）；`ff_theta_filter_ratio`、`ff_recalled_then_touched_rate` 有数

### M4：横切收尾——"可观测、可运营"（烟测准出）

- §19.6 最小集补齐核对（系统指标全量 + FF 骨架 + 留痕两指标）；§12.4 最小集（训练 topic + 加工 worker + R1–R3 + 撤回/销毁联动）
- 限流与错误码标定（§9 留待实施项）；runbook 骨架（§11.5 六条红线操作化）
- 验收（**烟测准出标准**）：四接口 + 指标大盘 + 告警可达 + runbook 骨架 + spike 基线回归全绿

### M5：烟测/种子期（非开发里程碑，阶段定位见 v1.5）

混沌工程场景清单执行（§11.4）、参数标定（§19.2 指标驱动）、§12.4 延后项（R4–R6、分层自动化）

### M6：阶段 1 Cell 化（**条件里程碑**）

§17/§18 在自有集群落地（per-space keyspace/index、调度器、EX 池）。**触发条件**：M4 准出 + 种子期反馈无架构级返工；若种子期暴露核心机制问题，M6 顺延——不为赶阶段路线牺牲 1.0 验证质量（§15.4 阶段切换由验证准出驱动，不由日历驱动）

## 5. 工程纪律（承接定案，开发期硬约束）

- 每个服务的"完成"定义 = 功能 + §19 指标 + 日志事件 + runbook 条目，缺一项不算完成
- EX 写路径的任何改动属 §11.2 升级清单第 1 条——必须人类决策岗评审
- 测试策略：spike 基线（集成）+ 服务单测；混沌场景在 M5 前不阻塞开发，但 M1–M4 验收均含"已知故障模式不扩散"检查（时钟偏移、依赖宕机时的失败即知，§16 踩坑录）
- 社区开发协同（§0.5.2）：里程碑为评审节点，每个里程碑结束更新设计文档（实现注记类修订走新版本号）

## 6. 风险登记

| 风险 | 应对 |
|---|---|
| SS 六维打分质量不达标（LLM 打分不稳定） | M2 前置验证；打分分布进 §19.2 观测；降级方案：规则+LLM 混合，单维度缺失时降级权重 |
| Pulsar 引入的组件复杂度（v0.7 已知权衡） | M0 起纳入混沌观察对象；backlog 指标先行 |
| consolidation 因果/实体边实现复杂度超预期 | M2 已定位为占位；完整实现不阻塞四接口交付 |
| 社区开发节奏不可控 | 里程碑不设死日期，设准出标准；M 间可并行的工作包显式标注（M4 横切可与 M3 并行启动） |

## 7. 明确不在本规划内

死排期与人月估算（社区开发形态不适用）；Cell 调度器产品化细节（M6 启动时展开）；2.0 任何题目；MS/Growth 任何工作。

---

*本规划评审通过后作为 1.0 开发执行依据；里程碑验收结果与实现注记按 §11.3 留痕，文档修订走主文档新版本号。*

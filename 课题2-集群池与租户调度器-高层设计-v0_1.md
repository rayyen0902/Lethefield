# Lethefield — 课题 2：集群池 + 租户调度器高层设计

**版本**: v0.1（提案，未定稿）
**状态**: 纸面论证稿——§10 课题 (2) 的产出，供评审；通过后以新版本号并入主文档
**关联**: Lethefield-RMS-设计文档 v1.1（§0.5、§13.4、§14、§15、§16、§11.5）
**日期**: 2026-07-29

---

## 1. 问题定义

§15.1 定案了 per-space 存储物理隔离：每个 space 一个 Cassandra keyspace + 一个 ES index。这把 §15.3 的容量约束推到台前：

- **Cassandra**：单集群稳定承载约 1000~2000 个 keyspace（元数据与 memtable 上限）
- **Elasticsearch**：分片堆开销是更紧约束——冷 space 降配（1 主 0 副本）后单集群数千个 index 量级
- 结论：超过单集群容量后，必须有**集群池 + 租户调度器**，把 space 水平分摊到多个对等集群

本设计同时受三条既有约束框定：

1. **§0.5.2 部署形态**：生产环境由多家第三方服务商**各自独立**部署运维——调度器是"每个部署实例一套"的控制面，不是全局唯一的；设计必须简单到服务商可独立正确部署（课题 3 的输入）
2. **§14 可携带性**：space 必须能整包迁移（集群间、服务商间），调度器不能让 space 锁死在某个集群
3. **§13.4 + §11.5 红线 5**：整 space 销毁有固定操作顺序（先隔离计算实例，再删存储）

## 2. 核心架构决策：Cell（单元）架构

**决策：Cassandra 集群与 ES 集群不成对独立调度，而是捆绑为 Cell——一个 Cell = 1 个 Cassandra 集群 + 1 个 ES 集群（+ 逻辑上关联的计算池分组），space 归属唯一 Cell。**

| 方案 | 判断 |
|---|---|
| **Cell 捆绑（采用）** | space 的两类存储（图 + 向量/索引）永远同 Cell：迁移原子（整 Cell 内空间搬迁不涉及跨 Cell 数据拼装）、故障域清晰（一个 Cell 挂了影响边界明确）、容量规划单一变量（Cell 水位）、服务商部署单元简单（Cell 是一套模板） |
| Cass 池与 ES 池独立调度（否决） | 一个 space 的 keyspace 在 Cass 集群 A、index 在 ES 集群 B，两维调度矩阵；迁移要协调两个独立决策，故障影响面变成笛卡尔积；节省的装箱灵活性在水位制分配下收益甚微，复杂度代价不成比例 |

**类比**：Cell 之于 Lethefield，等同可用区（AZ）之于云厂商——水平复制的标准单元，所有容量、调度、迁移、部署都以 Cell 为粒度。

### 2.1 三层结构

```
┌─────────────────────────────────────────────┐
│  控制面：租户调度器（无状态服务 + 元数据存储）   │
│  space→Cell 映射 / 开通 / 迁移 / 注销 / 水位    │
└─────────────────────────────────────────────┘
        │ 注册与发现              │ 连接路由
┌───────┴──────┐        ┌───────┴──────────────┐
│  计算池        │        │  存储层：Cell 池        │
│  JanusGraph   │───────▶│  Cell-1 … Cell-N      │
│  共享池 + LRU │        │  (Cass 集群 + ES 集群)   │
│  图实例缓存    │        │                       │
└──────────────┘        └───────────────────────┘
```

- **计算池**（§15.1 已定案）：共享 JanusGraph 池 + LRU 图实例缓存。graph open 时按调度器的 space→Cell 映射解析出目标 Cell 的连接端点；LRU 缓存键为 `(space_id, cell_id)`——Cell 迁移后旧缓存项自然失效（连接端点错误 → 重查映射 → 重开）
- **Cell 池**：对等 Cell 的水平集合，互相不感知、无跨 Cell 通信
- **控制面**：见 §3

## 3. 租户调度器（控制面）设计

### 3.1 元数据

调度器自身状态极小，核心是一张映射表：

```
space_id      → { cell_id, created_at, status(active|migrating|destroying), tier(hot|cold|premium) }
cell_id       → { cass_endpoints, es_endpoints, capacity: {keyspaces, indices, disk, heap},
                  watermark_state(open|filling|closed), version }
```

存储选型：**一个独立的小型 Cassandra 集群（或复用 Cell-1 的专用 keyspace）**。理由：技术栈内已有组件，服务商零新增运维负担；控制面数据量极小（百万 space 仅百 MB 级），单 keyspace 远未触及上限。接口做抽象（`ControlPlaneStore`），不阻止服务商换成其惯用的 etcd/云元数据库。

**红线传导**：调度器元数据不属于任何租户的记忆资产，但 `space→Cell` 映射丢失 = 全部 space 失联（数据还在，找不到入口）——映射表必须有备份与导出机制，纳入服务商部署规范（课题 3）。

### 3.2 开通（space 创建）

```
1. 选 Cell：从 watermark_state=open 的 Cell 中选水位最低者（确定性算法，无动态均衡）
2. 创建存储：JanusGraph createConfiguration（显式配置路径，§16.2 实测 ~0.9s）
             + ES 建 index（冷 space 直接降配：1 主 0 副本）
3. 注册映射：写 space→Cell（先存储后注册，失败回滚存储）
4. 预热（可选）：tier=hot/premium 的 space 触发计算池预 open，消化冷开 3.6s（§16.2）
```

### 3.3 水位制调度（刻意保持简单）

每个 Cell 周期性上报四维水位：keyspace 数 / ES 分片数 / 磁盘占用 / 堆压力。三档状态：

| 状态 | 条件（初值，待实测标定） | 行为 |
|---|---|---|
| open | 全部维度 < 70% | 接受新 space 分配 |
| filling | 任一维度 70%~90% | 停止新分配；触发新 Cell 筹备流程（人工或自动扩容） |
| closed | 任一维度 > 90% | 只出不进；告警 |

**明确不做的事**：

- **不做自动再平衡**（不因水位高而自动搬迁存量 space）——数据迁移是最重最贵的操作，自动触发等于把偶发容量问题放大成迁移风暴；再平衡只在新分配侧体现（水位高的 Cell 自然不再进新租户）
- **不做动态打分调度**（不综合 QPS/延迟/租户画像做复杂装箱）——§15.3 的容量约束是静态的（keyspace/分片数量），动态信息对分配决策边际收益低，复杂度代价高。若未来出现明确热点问题，用 tier 升级（cold→hot/premium）而非搬迁解决

### 3.4 迁移（space 跨 Cell 搬迁）

触发场景：Cell 退役、服务商间迁移（§14）、主动再平衡（人工拍板）。流程：

```
1. space 标记 migrating（读正常，写进入缓冲或短暂只读——窗口目标 < 1 分钟）
2. Cassandra：snapshot 源 keyspace → sstableloader 导入目标 Cell（或机房内直接流式）
3. ES：snapshot 源 index → restore 到目标 Cell（或 reindex remote）
4. 校验：行数/文档数/抽样比对
5. 切映射：space→新 Cell，恢复写
6. 延迟清理：源侧数据保留宽限期（如 7 天）后按 §13.4 流水线销毁
```

跨服务商场景退化为**文件级导出/导入**（§14 已定义整 space 导出权）：源侧导出 keyspace+index 快照包 → 用户携带 → 目标服务商导入为新 space。

### 3.5 注销（整 space 销毁，§13.4 的操作化）

严格按红线 5 顺序，五步带校验：

```
1. 标记 destroying，拒绝新请求
2. 计算池驱逐该 space 的图实例（先停实例，红线 5）
3. DROP KEYSPACE（对应 Cell 的 Cassandra）
4. DELETE index（对应 Cell 的 ES）
5. 清理 Redis 缓存与调度器映射，全链路校验无残留
```

### 3.6 调度器自身的高可用

调度器无状态（状态全在元数据存储），多副本部署即可；调度器宕机的影响面收敛为"不能开通/迁移/注销"——**存量 space 的读写不经过调度器**（计算池持有映射缓存 + Cell 端点直连），符合"控制面故障不扩散到数据面"原则。

## 4. 规模演算

以 1 Cell = 3 节点 Cassandra + 3 节点 ES 为标准单元（初值，待实测标定）：

| 规模 | Cell 数 | 说明 |
|---|---|---|
| ≤ 1,500 space | 1 | 单 Cell 即 §15.4 阶段 1 的起点形态 |
| 10 万 space | ~70–100 | 每 Cell 约 1,000–1,500 space（取 Cass 上限的保守侧） |
| 1 亿 space | ~7–10 万 | Cell 是纯水平复制，控制面元数据 ~GB 级，无瓶颈 |
| 数十亿（远景） | 百万级 Cell | 单服务商不会达到；多服务商分治后单部署实例规模受市场自然约束 |

关键判断：**瓶颈永远在 Cell 的供给速度（钱和机器），不在调度器**。调度器复杂度的上限因此可以被压得很低——这是好事，正是课题 3（服务商独立部署）需要的性质。

## 5. 与既有决策的一致性检查

- **§15.1**：计算共享池 + LRU 缓存不变；Cell 仅给存储层加归属维度，缓存键扩展为 `(space_id, cell_id)`——兼容
- **§15.2 四层模型**：tier 字段直接落在映射表；封存层 = 迁移流程的特例（目标端是对象存储而非 Cell）——兼容
- **§11.5 红线**：红线 1（禁跨 space 扫描）→ 调度器所有批量操作必须绑定显式 space 列表；红线 2（单 space 配额）→ 配额检查在开通与运行期都在 Cell 内执行；红线 5 → §3.5 已操作化——兼容且被强化
- **§14 可携带性**：§3.4 跨服务商导出/导入即其落地路径——直接支撑
- **§16 spike 数字**：create ~0.9s（开通流程的同步部分可接受）；冷开 3.6s → §3.2 预热策略的依据；容器重启 ~52s → Cell 节点故障恢复规划的输入
- **§0.5 公理 3**：Cell 架构使"结构性保证"的边界进一步收窄——一个 Cell 的损坏/误操作影响面物理封闭——方向一致

## 6. 待实测 / 待决项

1. **Cell 容量标定**：单 Cell（3+3 节点）真实承载上限压测——keyspace 数、ES 分片数、堆压力三条曲线的拐点实测（当前 1000–2000 / 数千为业界经验值，非本项目实测）
2. **迁移演练**：§3.4 流程端到端演练（含切映射瞬间的行为），测量只读窗口实际时长
3. **水位阈值**：70/90 两档为初值，待容量标定后调整
4. **计算池与 Cell 的亲和性**：本稿假设计算池全局共享、按映射连任意 Cell；跨机房部署时可能需要计算池按 Cell 分组（就近连接），待真实部署拓扑明确后补充
5. **Pulsar 的租户归属**：EX 事件流（Pulsar tenant/namespace，§7.5.2）是否也按 Cell 分片，还是独立的全局 Pulsar 集群——本稿未覆盖，建议列入后续课题（与 EX 存储的 Cassandra 集群 §7.3 一起考虑）

## 7. 本稿明确不包含

- Cell 内部的高可用细节（Cassandra/ES 集群自身的副本与机架策略，属部署规范——课题 3）
- 计费与配额的商业规则（信托侧题目）
- 调度器的 API 详细定义（定稿后补）

---

*本稿为课题论证产出，评审通过后以新版本号并入主文档（§15.4 阶段 1 的准出条件之一）。*
